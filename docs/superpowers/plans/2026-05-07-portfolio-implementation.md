# Portfólio atzingen.dev — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build and deploy a single-page bilingual professional portfolio at `atzingen.dev`, with curated content, full open-source repo index across 4 GitHub orgs, and dark refined-utility visual identity matching `fii-alert.atzingen.dev`.

**Architecture:** Static site (HTML5 + custom CSS + vanilla JS, zero framework). Content hydrated client-side from JSON files. Build script in Python uses `gh api` to refresh the public repo list. Hosted on `debian-dockers` (home network) with host-level nginx serving the `public/` folder directly + Let's Encrypt TLS. Deploy automated via GitHub Actions on push to `main`.

**Tech Stack:**
- HTML5 / CSS (custom, no framework) / Vanilla JS (ES2022, no transpilation)
- Google Fonts: Instrument Serif + Geist + JetBrains Mono
- Python 3 + `gh` CLI for build script (zero pip deps)
- nginx 1.22+ + certbot (already installed on `debian-dockers`)
- GitHub Actions for CI/CD

**Spec source of truth:** `docs/superpowers/specs/2026-05-07-portfolio-design.md`.

**Important deviation from spec:** The spec proposed `data/` (root) + `public/data/` (copied at build). To keep things simpler and avoid path bugs, this plan uses **only `public/data/`** (commit-tracked JSON files live where the browser fetches them; `build.py` adds `repos.json` and `build.json` on top, both gitignored). All other behavior matches the spec.

**Pré-condições antes de executar:**
- DNS apex já configurado: `A @ → 189.98.101.26 TTL 600` (confirmado pelo usuário; resolvendo como esperado).
- `www.atzingen.dev` ainda tem registros antigos AWS — limpeza tratada na Task 27.
- `fii-alert.atzingen.dev` em produção em `debian-dockers` (NÃO TOCAR — verificações de regressão estão no plano).
- `gh` CLI autenticado localmente.
- SSH alias `debian-dockers-deployer` configurado e funcional.

---

## File structure

```
atzingen-dev/
├── .editorconfig
├── .github/workflows/deploy.yml
├── .gitignore
├── Makefile
├── README.md
├── build.py
├── docs/
│   ├── DEPLOY.md
│   └── superpowers/
│       ├── specs/2026-05-07-portfolio-design.md   (already exists)
│       └── plans/2026-05-07-portfolio-implementation.md   (this file)
└── public/
    ├── index.html
    ├── robots.txt
    ├── assets/
    │   ├── rosto.jpeg
    │   ├── favicon.svg
    │   ├── og-image.png
    │   └── icons/
    │       ├── email.svg
    │       ├── github.svg
    │       ├── lattes.svg
    │       └── linkedin.svg
    ├── css/
    │   └── style.css
    ├── js/
    │   ├── i18n.js
    │   ├── main.js
    │   └── filter.js
    └── data/
        ├── profile.json        (committed)
        ├── projects.json       (committed)
        ├── publications.json   (committed)
        ├── i18n.json           (committed — UI strings)
        ├── repos.json          (gitignored — generated by build.py)
        └── build.json          (gitignored — generated by build.py)
```

**File responsibilities:**
- `public/index.html` — semantic skeleton with section anchors and `data-i18n` attributes; no inline content beyond labels.
- `public/css/style.css` — design tokens + layout + components; all custom (~700 lines target).
- `public/js/i18n.js` — translation dictionary loader + DOM swap on toggle.
- `public/js/main.js` — fetches JSON files and renders Hero, Sobre, Papéis, Projetos, Ensino, Publicações sections.
- `public/js/filter.js` — area-tag click handler that filters projects + repos table.
- `public/data/*.json` — content and translations.
- `build.py` — generates `repos.json` (via `gh api`) and `build.json` (SHA + timestamp).
- `Makefile` — `make build`, `make dev`, `make clean`.
- `.github/workflows/deploy.yml` — SSH push-to-deploy.

---

## Phase 1 — Repo scaffolding

### Task 1: Create directory tree, init git, add baseline files

**Files:**
- Create: `atzingen-dev/.gitignore`
- Create: `atzingen-dev/.editorconfig`
- Create: `atzingen-dev/README.md`
- Create folders: `public/{assets/icons,css,js,data}`, `.github/workflows`, `docs/`

- [ ] **Step 1: Create directory tree**

```bash
cd /c/Users/Gustavo/Desktop/dev/atzingen-dev
mkdir -p public/assets/icons public/css public/js public/data .github/workflows docs
```

- [ ] **Step 2: Write `.gitignore`**

```
# Generated artifacts
public/data/repos.json
public/data/build.json

# OS / editors
.DS_Store
Thumbs.db
.vscode/
.idea/

# Python
__pycache__/
*.pyc

# Local secrets
.env
.env.local
```

- [ ] **Step 3: Write `.editorconfig`**

```
root = true

[*]
charset = utf-8
end_of_line = lf
indent_style = space
indent_size = 2
insert_final_newline = true
trim_trailing_whitespace = true

[*.py]
indent_size = 4

[*.md]
trim_trailing_whitespace = false
```

- [ ] **Step 4: Write `README.md`**

```markdown
# atzingen.dev

Personal portfolio — single-page static site with bilingual content.

**Live:** https://atzingen.dev

## Stack
HTML + custom CSS + vanilla JS. Python build script generates the public-repo
index from `gh api` across `Atzingen`, `CEPAD-IFSP`, `inteligenciaatuarial`
and `Rede-DSBR`.

## Develop locally
```sh
make build      # generate repos.json from GitHub
make dev        # python -m http.server -d public 8080
```

Open http://localhost:8080.

## Edit content
- `public/data/profile.json` — bio, papéis, contatos (PT/EN)
- `public/data/projects.json` — featured projects
- `public/data/publications.json` — selected papers
- `public/data/i18n.json` — UI strings

## Deploy
GitHub Actions on push to `main` → SSH `debian-dockers-deployer` →
`git pull && make build && nginx -s reload`. Static files served direct by
host nginx from `/var/local/apps/atzingen-dev/public/`.

See `docs/DEPLOY.md` for the manual one-time setup.
```

- [ ] **Step 5: Initialize git and stage**

```bash
cd /c/Users/Gustavo/Desktop/dev/atzingen-dev
git init -b main
git add .gitignore .editorconfig README.md
git status   # expect: 3 files staged
```

- [ ] **Step 6: First commit**

```bash
git commit -m "chore: scaffold repo with baseline config files"
```

Expected: commit on `main`, no errors.

---

### Task 2: Copy photo + write favicon SVG + write contact icons

**Files:**
- Create: `public/assets/rosto.jpeg` (copied)
- Create: `public/assets/favicon.svg`
- Create: `public/assets/icons/email.svg`
- Create: `public/assets/icons/github.svg`
- Create: `public/assets/icons/lattes.svg`
- Create: `public/assets/icons/linkedin.svg`

- [ ] **Step 1: Copy the LinkedIn portrait**

```bash
cp "C:/Users/Gustavo/Utilitarios/rosto.jpeg" public/assets/rosto.jpeg
ls -la public/assets/rosto.jpeg
```

Expected: file exists, ~30-80 KB.

- [ ] **Step 2: Write `public/assets/favicon.svg`**

A monogram "G" in serif italic ouro on dark, matching brand:

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <rect width="32" height="32" fill="#0d0f12"/>
  <text x="50%" y="58%" font-family="Georgia, 'Times New Roman', serif"
        font-style="italic" font-size="22" font-weight="400"
        fill="#d4a14a" text-anchor="middle" dominant-baseline="middle">G</text>
</svg>
```

- [ ] **Step 3: Write `public/assets/icons/email.svg`**

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none"
     stroke="currentColor" stroke-width="1.5" stroke-linecap="round"
     stroke-linejoin="round">
  <rect x="3" y="5" width="18" height="14" rx="2"/>
  <path d="M3 7l9 6 9-6"/>
</svg>
```

- [ ] **Step 4: Write `public/assets/icons/github.svg`**

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
  <path d="M12 .5C5.65.5.5 5.65.5 12c0 5.08 3.29 9.39 7.86 10.91.58.1.79-.25.79-.56v-2.02c-3.2.69-3.87-1.36-3.87-1.36-.52-1.32-1.27-1.67-1.27-1.67-1.04-.71.08-.7.08-.7 1.15.08 1.76 1.18 1.76 1.18 1.02 1.75 2.68 1.24 3.34.95.1-.74.4-1.24.73-1.53-2.55-.29-5.24-1.27-5.24-5.66 0-1.25.45-2.27 1.18-3.07-.12-.29-.51-1.46.11-3.04 0 0 .96-.31 3.15 1.17.91-.25 1.89-.38 2.86-.38.97 0 1.95.13 2.86.38 2.18-1.48 3.14-1.17 3.14-1.17.62 1.58.23 2.75.11 3.04.74.8 1.18 1.82 1.18 3.07 0 4.4-2.69 5.37-5.25 5.65.41.36.78 1.06.78 2.13v3.16c0 .31.21.66.8.55 4.56-1.52 7.85-5.83 7.85-10.91C23.5 5.65 18.35.5 12 .5z"/>
</svg>
```

- [ ] **Step 5: Write `public/assets/icons/lattes.svg`**

(Stylised "L" in a circle — Lattes doesn't have a public official mark we can ship, so we use a clean monogram.)

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none"
     stroke="currentColor" stroke-width="1.5" stroke-linecap="round"
     stroke-linejoin="round">
  <circle cx="12" cy="12" r="10"/>
  <path d="M9 7v10h6" stroke-width="2"/>
</svg>
```

- [ ] **Step 6: Write `public/assets/icons/linkedin.svg`**

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
  <path d="M20.45 20.45h-3.55v-5.57c0-1.33-.03-3.04-1.86-3.04-1.86 0-2.14 1.45-2.14 2.95v5.66H9.36V9h3.41v1.56h.05c.48-.9 1.64-1.86 3.37-1.86 3.6 0 4.27 2.37 4.27 5.46v6.29zM5.34 7.43a2.06 2.06 0 1 1 0-4.13 2.06 2.06 0 0 1 0 4.13zM7.12 20.45H3.56V9h3.56v11.45zM22.22 0H1.77C.79 0 0 .77 0 1.72v20.56C0 23.23.79 24 1.77 24h20.45c.98 0 1.78-.77 1.78-1.72V1.72C24 .77 23.2 0 22.22 0z"/>
</svg>
```

- [ ] **Step 7: Stage and commit**

```bash
git add public/assets/
git commit -m "feat: add portrait, favicon, and contact icons"
```

---

### Task 3: Write Makefile

**Files:**
- Create: `Makefile`

- [ ] **Step 1: Write `Makefile`**

```makefile
.PHONY: build dev clean help

PYTHON ?= python
PORT   ?= 8080

help:
	@echo "make build   - generate public/data/repos.json + build.json"
	@echo "make dev     - serve public/ on http://localhost:$(PORT)"
	@echo "make clean   - remove generated artifacts"

build:
	$(PYTHON) build.py

dev:
	$(PYTHON) -m http.server -d public $(PORT)

clean:
	rm -f public/data/repos.json public/data/build.json
```

- [ ] **Step 2: Test the help target**

```bash
make help
```

Expected output: 3 lines listing build/dev/clean targets.

- [ ] **Step 3: Commit**

```bash
git add Makefile
git commit -m "feat: add Makefile with build/dev/clean targets"
```

---

## Phase 2 — Content (data files)

### Task 4: Write `public/data/profile.json`

**Files:**
- Create: `public/data/profile.json`

- [ ] **Step 1: Write the JSON**

```json
{
  "name": "Gustavo von Atzingen",
  "fullName": "Gustavo Voltani von Atzingen",
  "tagline": {
    "pt": "Professor · Pesquisador · Engenheiro",
    "en": "Professor · Researcher · Engineer"
  },
  "oneLiner": {
    "pt": "Computer vision, EEG, atuarial e sistemas para educação e governo.",
    "en": "Computer vision, EEG, actuarial and systems for education and government."
  },
  "base": "Piracicaba / SP",
  "education": [
    { "level": "PhD", "field": "Engenharia de Alimentos", "inst": "FZEA-USP", "year": 2017 },
    { "level": "MSc", "field": "Engenharia de Alimentos", "inst": "FZEA-USP", "year": 2013 },
    { "level": "BSc", "field": "Física",                  "inst": "IFSC-USP", "year": 2007 }
  ],
  "bio": {
    "pt": [
      "Físico de formação, engenheiro de alimentos por treino, e hoje muito mais. Bacharel em Física pela USP/IFSC (2007), com Mestrado e Doutorado em Engenharia de Alimentos pela FZEA-USP (2013/2017), trabalhando com instrumentação eletrônica e simulação numérica de processos térmicos. Sou professor no IFSP Campus Piracicaba desde então.",
      "Como pesquisador, trabalho com visão computacional, EEG, machine learning aplicado e instrumentação embarcada. No CEPAD-IFSP coordeno e participo de projetos de plataformas digitais para iniciativas governamentais — Plataforma Nilo Peçanha (rede federal CET/IF), dashboards de cestas básicas (MDS), agroecologia (MMA) — e fui coordenador do programa IFMAISEMPREENDEDOR em Piracicaba (2023, 2025).",
      "Em paralelo, fundei a Quickium e sou sócio da Inteligência Atuarial, onde construímos motores de cálculo e sistemas de gestão atuarial para RPPS. Filosofia: software que precisa rodar, ser mantido e gerar valor mensurável."
    ],
    "en": [
      "Physicist by training, food engineer by formation, and quite a bit more today. BSc in Physics from USP/IFSC (2007), with MSc and PhD in Food Engineering from FZEA-USP (2013/2017), working on electronic instrumentation and numerical simulation of thermal processes. Professor at IFSP Piracicaba ever since.",
      "As a researcher I work on computer vision, EEG, applied machine learning and embedded instrumentation. At CEPAD-IFSP I coordinate and contribute to digital-platform projects for government initiatives — the Plataforma Nilo Peçanha (Brazil's federal CET/IF data platform), food-basket dashboards (MDS), agroecology (MMA) — and I led the IFMAISEMPREENDEDOR program in Piracicaba (2023, 2025).",
      "In parallel I founded Quickium and I'm a partner at Inteligência Atuarial, where we build actuarial calculation engines and management systems for Brazilian public pension funds (RPPS). Philosophy: software that has to run, be maintained, and deliver measurable value."
    ]
  },
  "roles": [
    {
      "id": "ifsp",
      "org": "IFSP Piracicaba",
      "title": { "pt": "Professor", "en": "Professor" },
      "period": "2014 — atual",
      "url": "https://prc.ifsp.edu.br",
      "description": {
        "pt": "Disciplinas de Inteligência Artificial, Visão Computacional e Visão Computacional Embarcada. Material aberto em vários repositórios, incluindo curso FIC com 28⭐ e webinar de Visão Computacional Embarcada com 10⭐.",
        "en": "Courses in Artificial Intelligence, Computer Vision and Embedded Computer Vision. Open material across several repositories, including a 28-star FIC course and a 10-star Embedded Computer Vision webinar."
      },
      "projects": ["IA-FIC-IFSP", "Webinar-Visão-Computacional-Embarcada", "VisaoComputacionalEmbarcada-2024", "R-exams-Interface", "Oficinas_IFEMPREENDEDOR_2024"]
    },
    {
      "id": "cepad",
      "org": "CEPAD-IFSP",
      "title": { "pt": "Pesquisador", "en": "Researcher" },
      "period": "2018 — atual",
      "url": "https://cepad.digital",
      "description": {
        "pt": "Centro de Pesquisa em Análise de Dados do IFSP. Plataformas digitais e analytics para iniciativas governamentais em parceria com MDS, MMA, FNDE.",
        "en": "Data Analysis Research Centre at IFSP. Digital platforms and analytics for government initiatives in partnership with the Ministries of Social Development, Environment, and the FNDE."
      },
      "projects": ["alimentacidades", "dash-bacias", "agroecologiaMMA", "deserto-ia", "cozinhasolidaria", "agrotoxico-analytics", "Airflow-ETL", "nea-dashboard"]
    },
    {
      "id": "pnp",
      "org": "Plataforma Nilo Peçanha",
      "title": { "pt": "Cientista de Dados", "en": "Data Scientist" },
      "period": "2023 — atual",
      "url": "https://www.gov.br/pnp",
      "description": {
        "pt": "Plataforma federal oficial de dados da rede CET/IF (SETEC/MEC). Engenharia de dados, dashboards e ferramentas de extração.",
        "en": "Brazil's federal CET/IF data platform (SETEC/MEC). Data engineering, dashboards, extraction tooling."
      },
      "projects": ["kiosk-pnp", "pnp-web", "ExtratorV2", "pnp-monitor", "pnp_publicada"]
    },
    {
      "id": "quickium",
      "org": "Quickium",
      "title": { "pt": "Founder & CTO", "en": "Founder & CTO" },
      "period": "—",
      "url": "https://quickium.com",
      "description": {
        "pt": "Tecnologia e soluções customizadas. (Descrição completa pendente — ajuste em data/profile.json.)",
        "en": "Technology and custom solutions. (Full description pending — edit data/profile.json.)"
      },
      "projects": []
    },
    {
      "id": "iatuarial",
      "org": "Inteligência Atuarial",
      "title": { "pt": "Sócio", "en": "Partner" },
      "period": "—",
      "url": "https://inteligenciaatuarial.com.br",
      "description": {
        "pt": "Motores de cálculo e sistemas de gestão atuarial para Regimes Próprios de Previdência Social.",
        "en": "Actuarial calculation engines and management systems for Brazilian public-employee pension funds (RPPS)."
      },
      "projects": ["iaprev", "motor-ia", "portal-bases", "magma"]
    }
  ],
  "areas": [
    { "id": "ml",       "label": { "pt": "Machine Learning",   "en": "Machine Learning" } },
    { "id": "cv",       "label": { "pt": "Computer Vision",    "en": "Computer Vision" } },
    { "id": "cv-embed", "label": { "pt": "CV Embarcada",       "en": "Embedded CV" } },
    { "id": "eeg",      "label": { "pt": "EEG / Sinais",       "en": "EEG / Signals" } },
    { "id": "atuarial", "label": { "pt": "Atuarial / RPPS",    "en": "Actuarial / RPPS" } },
    { "id": "powerbi",  "label": { "pt": "Power BI / Data Eng","en": "Power BI / Data Eng" } },
    { "id": "ai-tools", "label": { "pt": "AI Dev Tools",       "en": "AI Dev Tools" } },
    { "id": "gov",      "label": { "pt": "Government Data",    "en": "Government Data" } }
  ],
  "teaching": {
    "courses": ["Inteligência Artificial", "Visão Computacional", "Visão Computacional Embarcada"],
    "openMaterial": ["IA-FIC-IFSP", "R-exams-Interface", "Oficinas_IFEMPREENDEDOR_2024"],
    "talks": ["Webinar-Visão-Computacional-Embarcada"],
    "programsCoordinated": ["IFMAISEMPREENDEDOR Piracicaba (2023)", "IFMAISEMPREENDEDOR Piracicaba (2025)"],
    "advisingActive": [
      "IC — EEG e tarefas cognitivas com IA (Eng. Computação)",
      "IC — EEG e qualidades de café com IA (Eng. Computação)",
      "IC — Corretor de Scripts (Eng. Computação)",
      "IA Embarcada (parceria Cubatão + Guarulhos)"
    ]
  },
  "contact": {
    "emails": [
      { "label": "IFSP",     "addr": "gustavo.von@ifsp.edu.br" },
      { "label": "Pessoal",  "addr": "gustavo.von.atzingen@gmail.com" }
    ],
    "github": [
      { "label": "Atzingen",          "url": "https://github.com/Atzingen" },
      { "label": "CEPAD-IFSP",        "url": "https://github.com/CEPAD-IFSP" },
      { "label": "inteligenciaatuarial","url": "https://github.com/inteligenciaatuarial" },
      { "label": "Rede-DSBR",         "url": "https://github.com/Rede-DSBR" }
    ],
    "lattes":   "http://lattes.cnpq.br/5173282107514295",
    "linkedin": "https://www.linkedin.com/in/gustavoatzingen/"
  }
}
```

- [ ] **Step 2: Validate JSON**

```bash
python -c "import json; json.load(open('public/data/profile.json', encoding='utf-8'))"
```

Expected: no output (valid JSON).

- [ ] **Step 3: Commit**

```bash
git add public/data/profile.json
git commit -m "feat(content): add profile.json with bio, roles and contacts (PT/EN)"
```

---

### Task 5: Write `public/data/projects.json`

**Files:**
- Create: `public/data/projects.json`

- [ ] **Step 1: Write the JSON**

```json
{
  "featured": [
    {
      "name": "hey-jarvis",
      "org": "Atzingen",
      "url": "https://github.com/Atzingen/hey-jarvis",
      "demo": null,
      "areas": ["ai-tools", "ml"],
      "stack": ["Python", "openWakeWord", "faster-whisper", "piper TTS"],
      "summary": {
        "pt": "Lançador de projetos por voz para Linux/Hyprland — abre layouts, conversa com Claude ou suspende.",
        "en": "Voice-activated dev launcher for Linux/Hyprland — opens project layouts, talks to Claude, or suspends."
      }
    },
    {
      "name": "IA-FIC-IFSP",
      "org": "Atzingen",
      "url": "https://github.com/Atzingen/IA-FIC-IFSP",
      "demo": null,
      "areas": ["ml", "cv"],
      "stack": ["Jupyter", "Python", "scikit-learn"],
      "summary": {
        "pt": "Material do curso FIC do IFSP Piracicaba — fundamentos de Inteligência Artificial em notebooks.",
        "en": "Course material for the IFSP Piracicaba FIC programme — Artificial Intelligence foundations in notebooks."
      }
    },
    {
      "name": "tmdl-parser",
      "org": "Atzingen",
      "url": "https://github.com/Atzingen/tmdl-parser",
      "demo": null,
      "areas": ["powerbi"],
      "stack": ["Python"],
      "summary": {
        "pt": "Biblioteca Python que faz parse de arquivos TMDL (Tabular Model Definition Language) do Power BI em estruturas tipadas.",
        "en": "Python library that parses Power BI TMDL (Tabular Model Definition Language) files into typed structures."
      }
    },
    {
      "name": "Webinar-Visão-Computacional-Embarcada",
      "org": "Atzingen",
      "url": "https://github.com/Atzingen/Webinar-Vis-o-computacional-Embarcada",
      "demo": null,
      "areas": ["cv", "cv-embed"],
      "stack": ["Jupyter", "OpenCV", "TensorFlow Lite"],
      "summary": {
        "pt": "Notebooks e slides do webinar sobre visão computacional rodando em hardware embarcado.",
        "en": "Notebooks and slides for the webinar on computer vision on embedded hardware."
      }
    },
    {
      "name": "fii-scanner",
      "org": "Atzingen",
      "url": "https://github.com/Atzingen/fii-scanner",
      "demo": "https://fii-alert.atzingen.dev",
      "areas": ["powerbi"],
      "stack": ["Python", "Flask", "yfinance", "Telegram Bot"],
      "summary": {
        "pt": "Scanner de FIIs da B3 com 4 sinais combinados (z-score, queda sem notícia, P/VP, DY) e alertas no Telegram.",
        "en": "B3 real-estate fund scanner combining 4 signals (z-score, unexplained drop, P/B, DY) with Telegram alerts."
      }
    },
    {
      "name": "EEG_Sweetners",
      "org": "Atzingen",
      "url": "https://github.com/Atzingen/EEG_Sweetners",
      "demo": null,
      "areas": ["eeg", "ml"],
      "stack": ["Python", "Jupyter", "TensorFlow"],
      "summary": {
        "pt": "Dataset e modelos para comparar o efeito de adoçantes vs açúcar em sinais de EEG. Base do paper 2022 em Frontiers in Nutrition.",
        "en": "Dataset and NN models comparing sweeteners vs sugar effects on EEG signals. Backed our 2022 Frontiers in Nutrition paper."
      }
    },
    {
      "name": "claude-monitor",
      "org": "Atzingen",
      "url": "https://github.com/Atzingen/claude-monitor",
      "demo": null,
      "areas": ["ai-tools"],
      "stack": ["Python", "Textual"],
      "summary": {
        "pt": "Dashboard TUI para acompanhar e interagir com instâncias de Claude Code rodando localmente.",
        "en": "TUI dashboard to monitor and interact with running Claude Code instances."
      }
    },
    {
      "name": "alimentacidades",
      "org": "CEPAD-IFSP",
      "url": "https://github.com/CEPAD-IFSP/alimentacidades",
      "demo": null,
      "areas": ["gov"],
      "stack": ["JavaScript", "Django", "PostgreSQL"],
      "summary": {
        "pt": "Plataforma de monitoramento alimentar municipal — dados públicos + dashboards.",
        "en": "Municipal food-monitoring platform — public data and dashboards."
      }
    },
    {
      "name": "iaprev",
      "org": "inteligenciaatuarial",
      "url": "https://github.com/inteligenciaatuarial/iaprev",
      "demo": null,
      "areas": ["atuarial", "gov"],
      "stack": ["Django", "Python"],
      "summary": {
        "pt": "Sistema de gestão de segurados RPPS — substituto moderno do ProtecWeb.",
        "en": "Member-management system for Brazilian public pension funds — modern replacement for ProtecWeb."
      }
    }
  ]
}
```

- [ ] **Step 2: Validate**

```bash
python -c "import json; d=json.load(open('public/data/projects.json',encoding='utf-8')); assert len(d['featured'])==9, len(d['featured'])"
```

Expected: no output (assertion holds).

- [ ] **Step 3: Commit**

```bash
git add public/data/projects.json
git commit -m "feat(content): add projects.json with 9 curated featured projects"
```

---

### Task 6: Write `public/data/publications.json`

**Files:**
- Create: `public/data/publications.json`

- [ ] **Step 1: Write the JSON (data extracted from Lattes via Escavador, 2026-05-07)**

```json
{
  "selected": [
    {
      "year": 2024,
      "title": "Detection of news written by the ChatGPT through authorship attribution performed by a bidirectional LSTM model",
      "venue": "Caderno Pedagógico",
      "vol": "v.21, e9236",
      "coauthors": ["IAQUINTA, A.F."],
      "isFirstAuthor": false,
      "url": null
    },
    {
      "year": 2023,
      "title": "Improving Behavior Monitoring of Free-Moving Dairy Cows Using Noninvasive Wireless EEG Approach and Digital Signal Processing Techniques",
      "venue": "Applied Sciences",
      "vol": "v.13, p.10722",
      "coauthors": ["Silva, A.C.S.", "Arce, A.I.C.", "Arteaga, H.", "Sarnighausen, V.C.R.", "Costa, E.J.X."],
      "isFirstAuthor": false,
      "url": "https://doi.org/10.3390/app132010722"
    },
    {
      "year": 2022,
      "title": "The convolutional neural network as a tool to classify electroencephalography data resulting from the consumption of juice sweetened with caloric or non-caloric sweeteners",
      "venue": "Frontiers in Nutrition",
      "vol": "v.9, p.1-14",
      "coauthors": ["Arteaga, H.", "Silva, A.R.", "Ortega, N.F.", "Costa, E.J.X.", "Silva, A.C.S."],
      "isFirstAuthor": true,
      "url": "https://doi.org/10.3389/fnut.2022.901333"
    },
    {
      "year": 2021,
      "title": "EEG Multipurpose Eye Blink Detector using convolutional neural network",
      "venue": "Research, Society and Development",
      "vol": "v.10, e335101522712",
      "coauthors": ["Iaquinta, A.F.", "Silva, A.C.S.", "Ferraz Jr, A.", "Toledo, J.M."],
      "isFirstAuthor": false,
      "url": null
    },
    {
      "year": 2020,
      "title": "Classificador de trajetos para robôs de corrida seguidores de linha utilizando deep learning",
      "venue": "Universitas (Mogi-Mirim)",
      "vol": "v.14, p.71",
      "coauthors": ["Ferraz, A.J.", "Pereira, G.M.", "Belgamo, A."],
      "isFirstAuthor": false,
      "url": null
    },
    {
      "year": 2019,
      "title": "Electronic Instrumentation and Computational Simulation to Evaluate the Combined Use of Microwave and Infrared Technologies for Reheating Biphasic Foods",
      "venue": "International Journal of Food Engineering",
      "vol": "v.1, p.1/0192-11",
      "coauthors": ["Piza, L.V.", "Costa, E.J.X."],
      "isFirstAuthor": false,
      "url": null
    },
    {
      "year": 2019,
      "title": "Viscosities and Densities of Fatty Alcohol Mixtures from 298.15 to 338.15 K: Estimation by Kay's Rule and Prediction by the UNIFAC-VISCO and GC-UNIMOD Group Contribution Methods",
      "venue": "Journal of Chemical and Engineering Data",
      "vol": "v.64, p.1937-1947",
      "coauthors": ["Pinto, C.N.", "Frugoli, G.Z.", "Florido, P.M.", "Rodrigues, C.E.C.", "Gonçalves, C.B."],
      "isFirstAuthor": false,
      "url": null
    },
    {
      "year": 2017,
      "title": "Real-Time Control System Based on the Values of Derivative of the Redox Potential Aiming Nitrogen Removal in a Sequencing Batch Reactor Applied in Treating Dairy Wastewater",
      "venue": "Water, Air and Soil Pollution",
      "vol": "v.228, p.17-34",
      "coauthors": ["Ribeiro, R.", "Lima, F.", "Okamoto, V.H.T.", "Arce, A.I.C.", "Tomamso, G.", "Costa, E.J.X."],
      "isFirstAuthor": false,
      "url": null
    },
    {
      "year": 2015,
      "title": "Lattice Boltzmann simulation of cafestol and kahweol extraction from green coffee beans in high-pressure system",
      "venue": "Journal of Food Engineering",
      "vol": "v.176, p.88-96",
      "coauthors": ["Rosa, R.H.", "Belandria, V.", "Oliveira, A.L.", "Bostyn, S.", "Rabi, J.A."],
      "isFirstAuthor": false,
      "url": null
    }
  ],
  "lattesUrl": "http://lattes.cnpq.br/5173282107514295"
}
```

- [ ] **Step 2: Validate**

```bash
python -c "import json; d=json.load(open('public/data/publications.json',encoding='utf-8')); assert len(d['selected'])==9; assert any(p['isFirstAuthor'] for p in d['selected'])"
```

Expected: no output.

- [ ] **Step 3: Commit**

```bash
git add public/data/publications.json
git commit -m "feat(content): add publications.json (9 selected papers 2015-2024)"
```

---

### Task 7: Write `public/data/i18n.json` (UI strings)

**Files:**
- Create: `public/data/i18n.json`

- [ ] **Step 1: Write the JSON**

```json
{
  "pt": {
    "nav.about": "Sobre",
    "nav.areas": "Áreas",
    "nav.roles": "Atuação",
    "nav.projects": "Projetos",
    "nav.teaching": "Ensino",
    "nav.publications": "Publicações",
    "nav.opensource": "Open Source",
    "nav.contact": "Contato",
    "hero.scrollHint": "rolar",
    "hero.contactLabel": "Contato direto",
    "section.about.title": "Sobre",
    "section.about.highlights": "Destaques",
    "section.areas.title": "Áreas de pesquisa & atuação",
    "section.areas.helper": "Clique em uma área para filtrar projetos e repositórios.",
    "section.roles.title": "Atuação",
    "section.projects.title": "Projetos em destaque",
    "section.projects.allAreas": "todas",
    "section.teaching.title": "Ensino, pesquisa & divulgação",
    "section.teaching.courses": "Disciplinas atuais",
    "section.teaching.openMaterial": "Material aberto",
    "section.teaching.talks": "Webinars e palestras",
    "section.teaching.coordinated": "Programas coordenados",
    "section.teaching.advising": "Orientações ativas",
    "section.publications.title": "Publicações selecionadas",
    "section.publications.firstAuthor": "primeiro autor",
    "section.publications.lattesCta": "Lista completa no Lattes →",
    "section.opensource.title": "Open source",
    "section.opensource.searchPlaceholder": "buscar repositório…",
    "section.opensource.recentOnly": "ativo nos últimos 12 meses",
    "section.opensource.cols.repo": "repositório",
    "section.opensource.cols.org": "organização",
    "section.opensource.cols.lang": "linguagem",
    "section.opensource.cols.stars": "⭐",
    "section.opensource.cols.updated": "atualizado",
    "section.opensource.cols.desc": "descrição",
    "section.opensource.empty": "Nenhum repositório bate com o filtro atual.",
    "section.opensource.showAll": "ver todos",
    "section.contact.title": "Contato",
    "section.contact.cta": "Aberto a parcerias de pesquisa, palestras e projetos.",
    "section.contact.emails": "E-mails",
    "section.contact.profiles": "Perfis",
    "footer.builtAt": "build",
    "lang.toggle.aria": "Alternar idioma"
  },
  "en": {
    "nav.about": "About",
    "nav.areas": "Areas",
    "nav.roles": "Roles",
    "nav.projects": "Projects",
    "nav.teaching": "Teaching",
    "nav.publications": "Publications",
    "nav.opensource": "Open Source",
    "nav.contact": "Contact",
    "hero.scrollHint": "scroll",
    "hero.contactLabel": "Direct contact",
    "section.about.title": "About",
    "section.about.highlights": "Highlights",
    "section.areas.title": "Research & practice areas",
    "section.areas.helper": "Click an area to filter projects and repositories.",
    "section.roles.title": "Roles",
    "section.projects.title": "Featured projects",
    "section.projects.allAreas": "all",
    "section.teaching.title": "Teaching, research & outreach",
    "section.teaching.courses": "Current courses",
    "section.teaching.openMaterial": "Open material",
    "section.teaching.talks": "Webinars and talks",
    "section.teaching.coordinated": "Programs coordinated",
    "section.teaching.advising": "Active advising",
    "section.publications.title": "Selected publications",
    "section.publications.firstAuthor": "first author",
    "section.publications.lattesCta": "Full list on Lattes →",
    "section.opensource.title": "Open source",
    "section.opensource.searchPlaceholder": "search repository…",
    "section.opensource.recentOnly": "active in the last 12 months",
    "section.opensource.cols.repo": "repository",
    "section.opensource.cols.org": "org",
    "section.opensource.cols.lang": "language",
    "section.opensource.cols.stars": "⭐",
    "section.opensource.cols.updated": "updated",
    "section.opensource.cols.desc": "description",
    "section.opensource.empty": "No repository matches the current filter.",
    "section.opensource.showAll": "show all",
    "section.contact.title": "Contact",
    "section.contact.cta": "Open to research partnerships, talks and projects.",
    "section.contact.emails": "E-mails",
    "section.contact.profiles": "Profiles",
    "footer.builtAt": "built",
    "lang.toggle.aria": "Toggle language"
  }
}
```

- [ ] **Step 2: Validate (both langs have same keys)**

```bash
python -c "import json; d=json.load(open('public/data/i18n.json',encoding='utf-8')); pt=set(d['pt']); en=set(d['en']); assert pt==en, ('pt-only', pt-en, 'en-only', en-pt)"
```

Expected: no output (key sets match).

- [ ] **Step 3: Commit**

```bash
git add public/data/i18n.json
git commit -m "feat(content): add i18n.json with UI string dictionary (PT/EN)"
```

---

## Phase 3 — Build pipeline

### Task 8: Write `build.py`

**Files:**
- Create: `build.py`

- [ ] **Step 1: Write the script**

```python
"""Generate public/data/repos.json and public/data/build.json.

Calls `gh api` for each org and merges results. Filters out forks and
archived repos. Sorted by stars desc, then updatedAt desc.
"""

from __future__ import annotations

import datetime as dt
import json
import os
import subprocess
import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parent
OUT_DIR = ROOT / "public" / "data"

# (kind, name)  →  gh API path
SOURCES: list[tuple[str, str]] = [
    ("user",  "Atzingen"),
    ("org",   "CEPAD-IFSP"),
    ("org",   "inteligenciaatuarial"),
    ("org",   "Rede-DSBR"),
]

FIELDS = [
    "name", "full_name", "description", "html_url",
    "stargazers_count", "forks_count", "language",
    "updated_at", "pushed_at", "fork", "archived", "owner",
]


def gh_repos(kind: str, name: str) -> list[dict]:
    """Fetch all public repos for a user or org via gh api with pagination."""
    endpoint = "users" if kind == "user" else "orgs"
    cmd = [
        "gh", "api",
        "--paginate",
        "-H", "Accept: application/vnd.github+json",
        f"/{endpoint}/{name}/repos?per_page=100&type=public",
    ]
    out = subprocess.run(cmd, check=True, capture_output=True, text=True, encoding="utf-8")
    # gh --paginate returns concatenated JSON arrays as a stream of objects;
    # easiest robust parse: replace "][" with "," to coalesce arrays.
    text = out.stdout.replace("][", ",")
    return json.loads(text)


def slim(repo: dict) -> dict:
    return {
        "name": repo["name"],
        "fullName": repo["full_name"],
        "org": repo["owner"]["login"],
        "description": repo.get("description") or "",
        "url": repo["html_url"],
        "stars": repo.get("stargazers_count", 0),
        "forks": repo.get("forks_count", 0),
        "language": repo.get("language") or "—",
        "updatedAt": repo.get("pushed_at") or repo.get("updated_at"),
    }


def main() -> int:
    OUT_DIR.mkdir(parents=True, exist_ok=True)
    all_repos: list[dict] = []
    for kind, name in SOURCES:
        try:
            raw = gh_repos(kind, name)
        except subprocess.CalledProcessError as exc:
            print(f"[build] gh api failed for {kind}/{name}: {exc.stderr}",
                  file=sys.stderr)
            return 1
        kept = [r for r in raw if not r.get("fork") and not r.get("archived")]
        print(f"[build] {kind}/{name}: {len(raw)} total, {len(kept)} after filter")
        all_repos.extend(slim(r) for r in kept)

    # Sort: stars desc, then updatedAt desc
    all_repos.sort(
        key=lambda r: (-r["stars"], r["updatedAt"] or ""),
        reverse=False,
    )
    # Sorted with stars negated, so order is correct; ensure updatedAt desc tiebreak:
    all_repos.sort(
        key=lambda r: (r["stars"], r["updatedAt"] or ""),
        reverse=True,
    )

    repos_out = OUT_DIR / "repos.json"
    repos_out.write_text(
        json.dumps({"repos": all_repos, "count": len(all_repos)},
                   ensure_ascii=False, indent=2),
        encoding="utf-8",
    )
    print(f"[build] wrote {repos_out} ({len(all_repos)} repos)")

    sha = os.environ.get("GITHUB_SHA")
    if not sha:
        try:
            sha = subprocess.run(
                ["git", "rev-parse", "HEAD"],
                check=True, capture_output=True, text=True, encoding="utf-8"
            ).stdout.strip()
        except subprocess.CalledProcessError:
            sha = "dev"

    build_out = OUT_DIR / "build.json"
    build_out.write_text(
        json.dumps({
            "sha": sha,
            "shortSha": sha[:7] if len(sha) >= 7 else sha,
            "builtAt": dt.datetime.now(dt.timezone.utc).isoformat(timespec="seconds"),
        }, indent=2),
        encoding="utf-8",
    )
    print(f"[build] wrote {build_out}")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 2: Run it**

```bash
python build.py
```

Expected:
- 4 lines `[build] <kind>/<name>: N total, M after filter`
- `[build] wrote public/data/repos.json (NN repos)` (NN ≥ 30)
- `[build] wrote public/data/build.json`

- [ ] **Step 3: Sanity-check the output**

```bash
python -c "import json; d=json.load(open('public/data/repos.json',encoding='utf-8')); print('count:', d['count']); print('top5:', [(r['name'],r['stars']) for r in d['repos'][:5]])"
```

Expected: count > 30; top5 includes `IA-FIC-IFSP` (28⭐) and `hey-jarvis` (13⭐) near the top.

- [ ] **Step 4: Commit**

```bash
git add build.py
git commit -m "feat(build): add build.py to generate repos.json and build.json from gh CLI"
```

---

## Phase 4 — Page foundation

### Task 9: Write `public/css/style.css` (tokens, reset, typography, body)

**Files:**
- Create: `public/css/style.css`

- [ ] **Step 1: Write the CSS**

```css
/* ============================================================
   atzingen.dev — refined dark utility (extends fii-scanner brand)
   ============================================================ */

:root {
  --bg:           #0d0f12;
  --bg-elev:      #15181d;
  --bg-elev-2:    #1c2026;
  --border:       #262a31;
  --border-strong:#363b44;

  --text:         #e8e9ea;
  --text-muted:   #9ca1a8;
  --text-dim:     #6b7079;

  --accent:       #d4a14a;
  --accent-soft:  rgba(212, 161, 74, 0.12);
  --accent-line:  rgba(212, 161, 74, 0.32);

  --good:         #4ade80;
  --bad:          #ef4444;
  --info:         #7aa2f7;

  --serif:        "Instrument Serif", Georgia, "Times New Roman", serif;
  --sans:         "Geist", -apple-system, BlinkMacSystemFont, "Helvetica Neue", sans-serif;
  --mono:         "JetBrains Mono", "SF Mono", Menlo, Consolas, monospace;

  --max-w: 1200px;
  --pad-x: clamp(20px, 4vw, 48px);
}

*, *::before, *::after { box-sizing: border-box; }

html, body {
  margin: 0;
  padding: 0;
  background: var(--bg);
  color: var(--text);
  font-family: var(--sans);
  font-size: 14.5px;
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  letter-spacing: 0.005em;
}

body {
  background-image:
    radial-gradient(1100px 600px at 80% -10%, rgba(212, 161, 74, 0.05), transparent 60%),
    radial-gradient(800px 600px at -10% 120%, rgba(122, 162, 247, 0.04), transparent 60%);
  min-height: 100vh;
}

a {
  color: var(--text);
  text-decoration: none;
  transition: color 120ms ease;
}
a:hover { color: var(--accent); }
a.subtle { color: var(--text-muted); }

h1, h2, h3, h4 {
  font-family: var(--serif);
  font-weight: 400;
  letter-spacing: -0.01em;
  margin: 0;
}
h1 { font-size: clamp(48px, 7vw, 92px); line-height: 1; }
h1 em { font-style: italic; color: var(--accent); }
h2 { font-size: clamp(28px, 3.4vw, 40px); line-height: 1.15; }
h3 { font-size: 20px; line-height: 1.3; }

.eyebrow {
  font-family: var(--sans);
  font-size: 11px;
  font-weight: 500;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: var(--text-dim);
}

p { margin: 0 0 1em; color: var(--text-muted); }
p strong { color: var(--text); font-weight: 500; }

button, input {
  font-family: inherit;
  font-size: inherit;
  color: inherit;
}

::selection {
  background: var(--accent-soft);
  color: var(--text);
}

.shell {
  max-width: var(--max-w);
  margin: 0 auto;
  padding: 0 var(--pad-x);
}

section {
  padding: clamp(80px, 10vw, 140px) 0;
  border-top: 1px solid var(--border);
}
section:first-of-type { border-top: 0; }

.section-head {
  display: flex;
  align-items: baseline;
  gap: 16px;
  margin-bottom: clamp(28px, 4vw, 56px);
}
.section-head .eyebrow { color: var(--accent); }

/* utility: subtle horizontal divider used inside cards */
.hr {
  height: 1px;
  background: var(--border);
  margin: 16px 0;
}
```

- [ ] **Step 2: Commit**

```bash
git add public/css/style.css
git commit -m "feat(css): add base tokens, reset, typography matching FII brand"
```

---

### Task 10: Write `public/index.html` shell

**Files:**
- Create: `public/index.html`

- [ ] **Step 1: Write the HTML skeleton (sections empty for now — populated by main.js later)**

```html
<!doctype html>
<html lang="pt-br">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Gustavo von Atzingen — Professor, pesquisador e engenheiro</title>
  <meta name="description" content="Portfólio do Prof. Gustavo Voltani von Atzingen — IFSP, CEPAD, Quickium, Inteligência Atuarial. Computer vision, EEG, machine learning, atuarial.">
  <link rel="icon" type="image/svg+xml" href="/assets/favicon.svg">

  <meta property="og:type" content="profile">
  <meta property="og:title" content="Gustavo von Atzingen">
  <meta property="og:description" content="Professor IFSP · Pesquisador CEPAD · Founder Quickium · Sócio Inteligência Atuarial">
  <meta property="og:url" content="https://atzingen.dev">
  <meta property="og:image" content="https://atzingen.dev/assets/og-image.png">
  <meta name="twitter:card" content="summary_large_image">

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Instrument+Serif:ital@0;1&family=Geist:wght@300;400;500;600&family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>

<header class="topbar">
  <div class="shell topbar-inner">
    <a class="brand" href="#top" aria-label="atzingen.dev">
      <em>von</em><span>·atzingen</span>
    </a>
    <nav class="nav" aria-label="seções">
      <a href="#sobre"        data-i18n="nav.about">Sobre</a>
      <a href="#areas"        data-i18n="nav.areas">Áreas</a>
      <a href="#papeis"       data-i18n="nav.roles">Atuação</a>
      <a href="#projetos"     data-i18n="nav.projects">Projetos</a>
      <a href="#publicacoes"  data-i18n="nav.publications">Publicações</a>
      <a href="#open-source"  data-i18n="nav.opensource">Open Source</a>
      <a href="#contato"      data-i18n="nav.contact">Contato</a>
    </nav>
    <button class="lang-toggle" type="button"
            aria-label="Toggle language" data-lang-toggle>
      <span data-lang-pt>PT</span> <span class="sep">|</span> <span data-lang-en>EN</span>
    </button>
  </div>
</header>

<main id="top">

  <section id="hero" class="hero">
    <div class="shell hero-grid">
      <div class="hero-portrait">
        <img src="/assets/rosto.jpeg" width="220" height="220"
             alt="Gustavo von Atzingen" loading="eager">
      </div>
      <div class="hero-text">
        <h1>
          Gustavo <em>von</em> Atzingen
        </h1>
        <p class="hero-tagline" data-bind="profile.tagline"></p>
        <p class="hero-oneliner" data-bind="profile.oneLiner"></p>
        <ul class="role-row" data-bind="profile.roles.compact"></ul>
        <ul class="contact-row" data-bind="profile.contact.compact"></ul>
      </div>
    </div>
    <a class="scroll-hint" href="#sobre">
      <span data-i18n="hero.scrollHint">rolar</span>
      <svg viewBox="0 0 24 24" width="14" height="14" fill="none"
           stroke="currentColor" stroke-width="1.5">
        <path d="M12 5v14M5 12l7 7 7-7"/>
      </svg>
    </a>
  </section>

  <section id="sobre">
    <div class="shell">
      <div class="section-head">
        <span class="eyebrow">01</span>
        <h2 data-i18n="section.about.title">Sobre</h2>
      </div>
      <div class="about-grid">
        <div class="about-prose" data-bind="profile.bio"></div>
        <aside class="about-side" data-bind="profile.highlights"></aside>
      </div>
    </div>
  </section>

  <section id="areas">
    <div class="shell">
      <div class="section-head">
        <span class="eyebrow">02</span>
        <h2 data-i18n="section.areas.title">Áreas de pesquisa & atuação</h2>
      </div>
      <p class="section-helper" data-i18n="section.areas.helper">Clique em uma área para filtrar projetos e repositórios.</p>
      <ul class="area-pills" data-bind="profile.areas"></ul>
    </div>
  </section>

  <section id="papeis">
    <div class="shell">
      <div class="section-head">
        <span class="eyebrow">03</span>
        <h2 data-i18n="section.roles.title">Atuação</h2>
      </div>
      <div class="roles-grid" data-bind="profile.roles.full"></div>
    </div>
  </section>

  <section id="projetos">
    <div class="shell">
      <div class="section-head">
        <span class="eyebrow">04</span>
        <h2 data-i18n="section.projects.title">Projetos em destaque</h2>
      </div>
      <div class="projects-grid" data-bind="projects.featured"></div>
    </div>
  </section>

  <section id="ensino">
    <div class="shell">
      <div class="section-head">
        <span class="eyebrow">05</span>
        <h2 data-i18n="section.teaching.title">Ensino, pesquisa & divulgação</h2>
      </div>
      <div class="teaching-grid" data-bind="profile.teaching"></div>
    </div>
  </section>

  <section id="publicacoes">
    <div class="shell">
      <div class="section-head">
        <span class="eyebrow">06</span>
        <h2 data-i18n="section.publications.title">Publicações selecionadas</h2>
      </div>
      <ol class="pub-list" data-bind="publications.selected"></ol>
      <p class="pub-cta">
        <a target="_blank" rel="noopener" data-bind="publications.lattesCta"></a>
      </p>
    </div>
  </section>

  <section id="open-source">
    <div class="shell">
      <div class="section-head">
        <span class="eyebrow">07</span>
        <h2 data-i18n="section.opensource.title">Open source</h2>
      </div>
      <div class="repo-controls">
        <input type="search" id="repo-search"
               data-i18n-placeholder="section.opensource.searchPlaceholder">
        <label class="recent-toggle">
          <input type="checkbox" id="repo-recent">
          <span data-i18n="section.opensource.recentOnly"></span>
        </label>
        <ul class="lang-pills" data-bind="repos.languages"></ul>
      </div>
      <div class="repo-table-wrap">
        <table class="repo-table">
          <thead>
            <tr>
              <th data-i18n="section.opensource.cols.repo">repositório</th>
              <th data-i18n="section.opensource.cols.org">organização</th>
              <th data-i18n="section.opensource.cols.lang">linguagem</th>
              <th data-i18n="section.opensource.cols.stars">⭐</th>
              <th data-i18n="section.opensource.cols.updated">atualizado</th>
              <th data-i18n="section.opensource.cols.desc">descrição</th>
            </tr>
          </thead>
          <tbody data-bind="repos.rows"></tbody>
        </table>
        <p class="repo-empty" hidden data-i18n="section.opensource.empty"></p>
      </div>
    </div>
  </section>

  <section id="contato">
    <div class="shell">
      <div class="section-head">
        <span class="eyebrow">08</span>
        <h2 data-i18n="section.contact.title">Contato</h2>
      </div>
      <p class="contact-cta" data-i18n="section.contact.cta"></p>
      <div class="contact-grid" data-bind="profile.contact.full"></div>
    </div>
  </section>

</main>

<footer class="foot">
  <div class="shell foot-inner">
    <span>© <span data-bind="footer.year"></span> Gustavo von Atzingen</span>
    <span><span data-i18n="footer.builtAt">build</span> <a data-bind="footer.build"></a></span>
  </div>
</footer>

<script src="/js/i18n.js" defer></script>
<script src="/js/main.js" defer></script>
<script src="/js/filter.js" defer></script>

</body>
</html>
```

- [ ] **Step 2: Write `public/robots.txt`**

```
User-agent: *
Allow: /

Sitemap: https://atzingen.dev/sitemap.xml
```

- [ ] **Step 3: Commit**

```bash
git add public/index.html public/robots.txt
git commit -m "feat(html): add page skeleton with section anchors and data-bind hooks"
```

---

### Task 11: Write `public/js/i18n.js`

**Files:**
- Create: `public/js/i18n.js`

- [ ] **Step 1: Write the JS**

```javascript
/* atzingen.dev — i18n
 * Loads /data/i18n.json, applies translations to nodes carrying:
 *   data-i18n           → element.textContent
 *   data-i18n-placeholder → element.placeholder
 *   data-i18n-aria      → element.setAttribute("aria-label", ...)
 * Persists choice in localStorage["atzingen.lang"].
 *
 * Exposes window.atzI18n with: get(key), setLang(lang), lang, dict.
 * Fires a "atzingen:lang" event after each language switch so other
 * scripts (main.js) can re-render localised content.
 */

(function () {
  "use strict";

  const STORAGE_KEY = "atzingen.lang";
  const SUPPORTED = ["pt", "en"];

  function detectInitialLang() {
    const stored = localStorage.getItem(STORAGE_KEY);
    if (stored && SUPPORTED.includes(stored)) return stored;
    const nav = (navigator.language || "pt").toLowerCase();
    return nav.startsWith("en") ? "en" : "pt";
  }

  const state = {
    lang: detectInitialLang(),
    dict: null,
  };

  function applyTranslations() {
    if (!state.dict) return;
    const t = state.dict[state.lang] || {};
    document.documentElement.lang = state.lang === "pt" ? "pt-br" : "en";

    document.querySelectorAll("[data-i18n]").forEach((el) => {
      const key = el.dataset.i18n;
      if (t[key] !== undefined) el.textContent = t[key];
    });
    document.querySelectorAll("[data-i18n-placeholder]").forEach((el) => {
      const key = el.dataset.i18nPlaceholder;
      if (t[key] !== undefined) el.placeholder = t[key];
    });
    document.querySelectorAll("[data-i18n-aria]").forEach((el) => {
      const key = el.dataset.i18nAria;
      if (t[key] !== undefined) el.setAttribute("aria-label", t[key]);
    });

    document.querySelectorAll("[data-lang-toggle]").forEach((btn) => {
      btn.querySelector("[data-lang-pt]")?.classList.toggle("active", state.lang === "pt");
      btn.querySelector("[data-lang-en]")?.classList.toggle("active", state.lang === "en");
    });
  }

  function setLang(lang) {
    if (!SUPPORTED.includes(lang)) return;
    state.lang = lang;
    localStorage.setItem(STORAGE_KEY, lang);
    applyTranslations();
    document.dispatchEvent(new CustomEvent("atzingen:lang", { detail: { lang } }));
  }

  async function init() {
    try {
      const res = await fetch("/data/i18n.json", { cache: "no-cache" });
      state.dict = await res.json();
    } catch (err) {
      console.error("[i18n] failed to load /data/i18n.json", err);
      return;
    }
    applyTranslations();
    document.querySelectorAll("[data-lang-toggle]").forEach((btn) => {
      btn.addEventListener("click", () => {
        setLang(state.lang === "pt" ? "en" : "pt");
      });
    });
    document.dispatchEvent(new CustomEvent("atzingen:lang", { detail: { lang: state.lang } }));
  }

  window.atzI18n = {
    get lang() { return state.lang; },
    get dict() { return state.dict; },
    get: (key) => (state.dict?.[state.lang] || {})[key],
    setLang,
  };

  document.addEventListener("DOMContentLoaded", init);
})();
```

- [ ] **Step 2: Commit**

```bash
git add public/js/i18n.js
git commit -m "feat(js): add i18n loader with PT/EN toggle and persistence"
```

---

### Task 12: Write `public/js/main.js` (hydration of all sections)

**Files:**
- Create: `public/js/main.js`

- [ ] **Step 1: Write the JS**

```javascript
/* atzingen.dev — main
 * Loads profile.json, projects.json, publications.json, repos.json,
 * build.json and renders sections by [data-bind] attribute.
 * Re-renders content that depends on language on "atzingen:lang" event.
 */

(function () {
  "use strict";

  const state = {
    profile: null,
    projects: null,
    publications: null,
    repos: null,
    build: null,
  };

  const $ = (sel, root = document) => root.querySelector(sel);
  const $$ = (sel, root = document) => Array.from(root.querySelectorAll(sel));

  // ---------- helpers ----------

  function lang() { return window.atzI18n?.lang || "pt"; }
  function t(key) { return window.atzI18n?.get(key) || key; }
  function pick(obj) {
    if (obj && typeof obj === "object" && (obj.pt || obj.en)) return obj[lang()] ?? obj.pt;
    return obj;
  }
  function el(tag, attrs = {}, children = []) {
    const node = document.createElement(tag);
    for (const [k, v] of Object.entries(attrs)) {
      if (v === false || v == null) continue;
      if (k === "class") node.className = v;
      else if (k === "html") node.innerHTML = v;
      else if (k.startsWith("on") && typeof v === "function") {
        node.addEventListener(k.slice(2).toLowerCase(), v);
      } else node.setAttribute(k, v);
    }
    (Array.isArray(children) ? children : [children]).forEach((c) => {
      if (c == null || c === false) return;
      node.appendChild(c instanceof Node ? c : document.createTextNode(String(c)));
    });
    return node;
  }
  function fmtDate(iso) {
    if (!iso) return "—";
    const d = new Date(iso);
    if (Number.isNaN(d.getTime())) return "—";
    return d.toISOString().slice(0, 10);
  }

  // ---------- renderers ----------

  function renderHero() {
    const p = state.profile;
    if (!p) return;
    $("[data-bind='profile.tagline']").textContent = pick(p.tagline);
    $("[data-bind='profile.oneLiner']").textContent = pick(p.oneLiner);

    const roleRow = $("[data-bind='profile.roles.compact']");
    roleRow.innerHTML = "";
    p.roles.forEach((r, i) => {
      if (i > 0) roleRow.appendChild(el("li", { class: "sep" }, "·"));
      roleRow.appendChild(el("li", { class: "role-chip" }, [
        el("span", { class: "role-org" }, r.org),
        el("span", { class: "role-title" }, pick(r.title)),
      ]));
    });

    const contact = $("[data-bind='profile.contact.compact']");
    contact.innerHTML = "";
    const items = [
      { icon: "email",    label: "email",    href: `mailto:${p.contact.emails[0].addr}` },
      { icon: "github",   label: "github",   href: p.contact.github[0].url },
      { icon: "lattes",   label: "lattes",   href: p.contact.lattes },
      { icon: "linkedin", label: "linkedin", href: p.contact.linkedin },
    ];
    items.forEach((it) => {
      contact.appendChild(el("li", {}, el("a", {
        href: it.href, target: it.href.startsWith("mailto:") ? null : "_blank",
        rel: "noopener", "aria-label": it.label, class: "icon-link",
      }, [
        el("img", { src: `/assets/icons/${it.icon}.svg`, alt: "", width: 18, height: 18 }),
      ])));
    });
  }

  function renderAbout() {
    const p = state.profile;
    const prose = $("[data-bind='profile.bio']");
    prose.innerHTML = "";
    pick(p.bio).forEach((para) => prose.appendChild(el("p", {}, para)));

    const side = $("[data-bind='profile.highlights']");
    side.innerHTML = "";
    side.appendChild(el("h3", { class: "eyebrow" }, t("section.about.highlights")));
    const hl = el("ul", { class: "highlights" }, [
      el("li", {}, [
        el("span", { class: "key" }, lang() === "pt" ? "Formação" : "Education"),
        el("span", {}, p.education.map((e) => `${e.level} ${e.inst} (${e.year})`).join(" · ")),
      ]),
      el("li", {}, [
        el("span", { class: "key" }, lang() === "pt" ? "Base" : "Based in"),
        el("span", {}, p.base),
      ]),
      el("li", {}, [
        el("span", { class: "key" }, lang() === "pt" ? "Áreas" : "Areas"),
        el("span", {}, p.areas.map((a) => pick(a.label)).join(" · ")),
      ]),
      el("li", {}, [
        el("span", { class: "key" }, "Lattes"),
        el("a", { href: p.contact.lattes, target: "_blank", rel: "noopener" }, "lattes.cnpq.br"),
      ]),
      el("li", {}, [
        el("span", { class: "key" }, "LinkedIn"),
        el("a", { href: p.contact.linkedin, target: "_blank", rel: "noopener" }, "/in/gustavoatzingen"),
      ]),
    ]);
    side.appendChild(hl);
  }

  function renderAreas() {
    const ul = $("[data-bind='profile.areas']");
    ul.innerHTML = "";
    ul.appendChild(el("li", {}, el("button", {
      type: "button", class: "area-pill is-active", "data-area": "all",
    }, t("section.projects.allAreas"))));
    state.profile.areas.forEach((a) => {
      ul.appendChild(el("li", {}, el("button", {
        type: "button", class: "area-pill", "data-area": a.id,
      }, pick(a.label))));
    });
  }

  function renderRoles() {
    const grid = $("[data-bind='profile.roles.full']");
    grid.innerHTML = "";
    state.profile.roles.forEach((r) => {
      const card = el("article", { class: "role-card", "data-role": r.id });
      card.appendChild(el("header", {}, [
        el("h3", {}, r.org),
        el("span", { class: "role-period" }, r.period),
      ]));
      card.appendChild(el("div", { class: "role-title" }, pick(r.title)));
      card.appendChild(el("p", {}, pick(r.description)));
      if (r.projects && r.projects.length) {
        const chips = el("ul", { class: "chip-row" });
        r.projects.forEach((name) => {
          const repo = state.repos?.repos.find((rr) => rr.name === name);
          const url = repo ? repo.url : `https://github.com/search?q=${encodeURIComponent(name)}&type=repositories`;
          chips.appendChild(el("li", {}, el("a", { href: url, target: "_blank", rel: "noopener", class: "chip" }, name)));
        });
        card.appendChild(chips);
      }
      if (r.url) {
        card.appendChild(el("a", {
          href: r.url, target: "_blank", rel: "noopener", class: "role-link",
        }, r.url.replace(/^https?:\/\//, "")));
      }
      grid.appendChild(card);
    });
  }

  function renderProjects() {
    const grid = $("[data-bind='projects.featured']");
    grid.innerHTML = "";
    state.projects.featured.forEach((p) => {
      const repo = state.repos?.repos.find((r) => r.name === p.name && r.org === p.org);
      const stars = repo?.stars ?? 0;
      const lng = repo?.language || (p.stack && p.stack[0]) || "—";
      const card = el("article", {
        class: "project-card", "data-areas": (p.areas || []).join(" "),
      }, [
        el("div", { class: "project-tags" },
          (p.areas || []).map((a) => el("span", { class: "tag tag-area", "data-area": a }, a))),
        el("h3", {}, el("a", { href: p.url, target: "_blank", rel: "noopener" }, p.name)),
        el("p", {}, pick(p.summary)),
        el("ul", { class: "stack-row" },
          (p.stack || []).map((s) => el("li", { class: "tag tag-mono" }, s))),
        el("footer", { class: "project-foot" }, [
          el("span", { class: "tag tag-mono" }, lng),
          stars > 0 ? el("span", { class: "stars" }, `★ ${stars}`) : null,
          el("a", { href: p.url, target: "_blank", rel: "noopener" }, "GitHub"),
          p.demo ? el("a", { href: p.demo, target: "_blank", rel: "noopener", class: "demo-link" }, "demo") : null,
        ]),
      ]);
      grid.appendChild(card);
    });
  }

  function renderTeaching() {
    const grid = $("[data-bind='profile.teaching']");
    grid.innerHTML = "";
    const tg = state.profile.teaching;
    const blocks = [
      { key: "section.teaching.courses",       items: tg.courses },
      { key: "section.teaching.openMaterial",  items: tg.openMaterial },
      { key: "section.teaching.talks",         items: tg.talks },
      { key: "section.teaching.coordinated",   items: tg.programsCoordinated },
      { key: "section.teaching.advising",      items: tg.advisingActive },
    ];
    blocks.forEach((b) => {
      grid.appendChild(el("section", { class: "teach-block" }, [
        el("h3", { class: "eyebrow" }, t(b.key)),
        el("ul", {}, (b.items || []).map((i) => el("li", {}, i))),
      ]));
    });
  }

  function renderPublications() {
    const list = $("[data-bind='publications.selected']");
    list.innerHTML = "";
    state.publications.selected.forEach((p) => {
      const li = el("li", { class: "pub-item" }, [
        el("span", { class: "pub-year" }, String(p.year)),
        el("div", { class: "pub-body" }, [
          el("div", { class: "pub-title" },
            p.url ? el("a", { href: p.url, target: "_blank", rel: "noopener" }, p.title) : p.title,
          ),
          el("div", { class: "pub-meta" }, [
            el("span", { class: "pub-venue" }, p.venue + (p.vol ? ` · ${p.vol}` : "")),
            p.isFirstAuthor ? el("span", { class: "tag tag-mono pub-first" }, t("section.publications.firstAuthor")) : null,
          ]),
          el("div", { class: "pub-authors" }, p.coauthors.join(" · ")),
        ]),
      ]);
      list.appendChild(li);
    });
    const cta = $("[data-bind='publications.lattesCta']");
    cta.href = state.publications.lattesUrl;
    cta.textContent = t("section.publications.lattesCta");
  }

  function renderContact() {
    const grid = $("[data-bind='profile.contact.full']");
    grid.innerHTML = "";
    const c = state.profile.contact;

    const emails = el("div", {}, [
      el("h3", { class: "eyebrow" }, t("section.contact.emails")),
      el("ul", { class: "contact-list" },
        c.emails.map((e) => el("li", {}, [
          el("span", { class: "key" }, e.label),
          el("a", { href: `mailto:${e.addr}` }, e.addr),
        ]))),
    ]);

    const profiles = el("div", {}, [
      el("h3", { class: "eyebrow" }, t("section.contact.profiles")),
      el("ul", { class: "contact-list" }, [
        ...c.github.map((g) => el("li", {}, [
          el("span", { class: "key" }, "GitHub"),
          el("a", { href: g.url, target: "_blank", rel: "noopener" }, g.label),
        ])),
        el("li", {}, [
          el("span", { class: "key" }, "Lattes"),
          el("a", { href: c.lattes, target: "_blank", rel: "noopener" }, "lattes.cnpq.br/5173282107514295"),
        ]),
        el("li", {}, [
          el("span", { class: "key" }, "LinkedIn"),
          el("a", { href: c.linkedin, target: "_blank", rel: "noopener" }, "/in/gustavoatzingen"),
        ]),
      ]),
    ]);

    grid.appendChild(emails);
    grid.appendChild(profiles);
  }

  function renderRepos() {
    if (!state.repos) return;
    const tbody = $("[data-bind='repos.rows']");
    tbody.innerHTML = "";
    state.repos.repos.forEach((r) => {
      const tr = el("tr", { "data-lang": r.language, "data-org": r.org, "data-updated": r.updatedAt }, [
        el("td", {}, el("a", { href: r.url, target: "_blank", rel: "noopener", class: "mono" }, r.name)),
        el("td", { class: "mono dim" }, r.org),
        el("td", { class: "mono" }, r.language),
        el("td", { class: "mono num" }, r.stars > 0 ? String(r.stars) : ""),
        el("td", { class: "mono dim" }, fmtDate(r.updatedAt)),
        el("td", {}, r.description || ""),
      ]);
      tbody.appendChild(tr);
    });

    const langs = $("[data-bind='repos.languages']");
    langs.innerHTML = "";
    const all = el("li", {}, el("button", {
      type: "button", class: "lang-pill is-active", "data-lang-filter": "*",
    }, lang() === "pt" ? "todas" : "all"));
    langs.appendChild(all);
    const counts = new Map();
    state.repos.repos.forEach((r) => counts.set(r.language, (counts.get(r.language) || 0) + 1));
    Array.from(counts.entries())
      .sort((a, b) => b[1] - a[1])
      .slice(0, 8)
      .forEach(([lng, n]) => {
        langs.appendChild(el("li", {}, el("button", {
          type: "button", class: "lang-pill", "data-lang-filter": lng,
        }, `${lng} (${n})`)));
      });
  }

  function renderFooter() {
    $("[data-bind='footer.year']").textContent = String(new Date().getFullYear());
    const a = $("[data-bind='footer.build']");
    if (a && state.build) {
      a.textContent = state.build.shortSha;
      a.href = `https://github.com/Atzingen/atzingen-dev/commit/${state.build.sha}`;
      a.target = "_blank";
      a.rel = "noopener";
    }
  }

  function renderAll() {
    renderHero();
    renderAbout();
    renderAreas();
    renderRoles();
    renderProjects();
    renderTeaching();
    renderPublications();
    renderContact();
    renderRepos();
    renderFooter();
  }

  // ---------- boot ----------

  async function loadJSON(url, optional = false) {
    try {
      const res = await fetch(url, { cache: "no-cache" });
      if (!res.ok) throw new Error(res.statusText);
      return await res.json();
    } catch (err) {
      if (optional) return null;
      console.error(`[main] failed to load ${url}`, err);
      return null;
    }
  }

  async function boot() {
    const [profile, projects, publications, repos, build] = await Promise.all([
      loadJSON("/data/profile.json"),
      loadJSON("/data/projects.json"),
      loadJSON("/data/publications.json"),
      loadJSON("/data/repos.json", true),
      loadJSON("/data/build.json", true),
    ]);
    state.profile = profile;
    state.projects = projects;
    state.publications = publications;
    state.repos = repos;
    state.build = build;
    renderAll();
  }

  document.addEventListener("DOMContentLoaded", () => {
    if (window.atzI18n?.dict) boot();
    else document.addEventListener("atzingen:lang", () => {
      if (!state.profile) boot();
      else renderAll();
    }, { once: false });
  });
})();
```

- [ ] **Step 2: Commit**

```bash
git add public/js/main.js
git commit -m "feat(js): hydrate sections from JSON files with re-render on lang switch"
```

---

### Task 13: Write `public/js/filter.js` (area tag → projects + repos filter)

**Files:**
- Create: `public/js/filter.js`

- [ ] **Step 1: Write the JS**

```javascript
/* atzingen.dev — filter
 * Listens for clicks on .area-pill and .lang-pill (open-source section)
 * and filters .project-card and .repo-table rows accordingly. Also wires
 * the search input and the "active in last 12 months" checkbox.
 */

(function () {
  "use strict";

  const state = {
    area: "all",
    repoLang: "*",
    repoSearch: "",
    repoRecentOnly: false,
  };

  const $ = (sel, root = document) => root.querySelector(sel);
  const $$ = (sel, root = document) => Array.from(root.querySelectorAll(sel));

  function applyAreaFilter() {
    $$(".project-card").forEach((card) => {
      const areas = (card.dataset.areas || "").split(" ").filter(Boolean);
      const show = state.area === "all" || areas.includes(state.area);
      card.toggleAttribute("hidden", !show);
    });
    $$(".area-pill").forEach((b) => b.classList.toggle("is-active", b.dataset.area === state.area));
  }

  function applyRepoFilters() {
    const cutoff = Date.now() - 365 * 24 * 60 * 60 * 1000;
    const q = state.repoSearch.toLowerCase().trim();

    let visible = 0;
    $$(".repo-table tbody tr").forEach((tr) => {
      const lang = tr.dataset.lang || "";
      const updated = Date.parse(tr.dataset.updated || "");
      const text = tr.textContent.toLowerCase();
      const matchLang = state.repoLang === "*" || lang === state.repoLang;
      const matchSearch = !q || text.includes(q);
      const matchRecent = !state.repoRecentOnly || (updated && updated >= cutoff);
      const show = matchLang && matchSearch && matchRecent;
      tr.toggleAttribute("hidden", !show);
      if (show) visible++;
    });
    const empty = $(".repo-empty");
    if (empty) empty.toggleAttribute("hidden", visible !== 0);
    $$(".lang-pill").forEach((b) =>
      b.classList.toggle("is-active", b.dataset.langFilter === state.repoLang));
  }

  function init() {
    document.addEventListener("click", (ev) => {
      const area = ev.target.closest(".area-pill");
      if (area) {
        state.area = area.dataset.area;
        applyAreaFilter();
        return;
      }
      const lang = ev.target.closest(".lang-pill");
      if (lang) {
        state.repoLang = lang.dataset.langFilter;
        applyRepoFilters();
        return;
      }
    });

    const search = $("#repo-search");
    if (search) {
      search.addEventListener("input", (ev) => {
        state.repoSearch = ev.target.value;
        applyRepoFilters();
      });
    }
    const recent = $("#repo-recent");
    if (recent) {
      recent.addEventListener("change", (ev) => {
        state.repoRecentOnly = ev.target.checked;
        applyRepoFilters();
      });
    }

    document.addEventListener("atzingen:lang", () => {
      // pills re-rendered by main.js; reapply current state
      requestAnimationFrame(() => {
        applyAreaFilter();
        applyRepoFilters();
      });
    });
  }

  document.addEventListener("DOMContentLoaded", init);
})();
```

- [ ] **Step 2: Commit**

```bash
git add public/js/filter.js
git commit -m "feat(js): add area/repo filter wiring (tag click, search, recent toggle)"
```

---

## Phase 5 — Visual styling for sections

### Task 14: Extend `public/css/style.css` — topbar + hero

**Files:**
- Modify: `public/css/style.css` (append)

- [ ] **Step 1: Append topbar + hero CSS**

```css
/* ============================================================
   Topbar
   ============================================================ */

.topbar {
  position: sticky;
  top: 0;
  z-index: 50;
  background: rgba(13, 15, 18, 0.78);
  backdrop-filter: saturate(140%) blur(8px);
  -webkit-backdrop-filter: saturate(140%) blur(8px);
  border-bottom: 1px solid var(--border);
}
.topbar-inner {
  display: flex;
  align-items: center;
  gap: 24px;
  height: 60px;
}
.brand {
  font-family: var(--serif);
  font-size: 18px;
  letter-spacing: 0;
  color: var(--text);
  display: inline-flex;
  align-items: baseline;
  gap: 2px;
}
.brand em {
  font-style: italic;
  color: var(--accent);
  margin-right: 1px;
}
.brand span { color: var(--text-muted); font-family: var(--mono); font-size: 13px; }

.nav {
  margin-left: auto;
  display: flex;
  gap: 22px;
}
.nav a {
  color: var(--text-muted);
  font-size: 13px;
  transition: color 120ms ease;
}
.nav a:hover { color: var(--text); }

.lang-toggle {
  background: transparent;
  border: 1px solid var(--border);
  color: var(--text-muted);
  border-radius: 6px;
  padding: 4px 10px;
  font-family: var(--mono);
  font-size: 12px;
  letter-spacing: 0.06em;
  cursor: pointer;
  transition: border-color 120ms ease, color 120ms ease;
}
.lang-toggle:hover { border-color: var(--border-strong); color: var(--text); }
.lang-toggle .sep { color: var(--border-strong); margin: 0 4px; }
.lang-toggle .active { color: var(--accent); }

@media (max-width: 720px) {
  .nav { display: none; }
}

/* ============================================================
   Hero
   ============================================================ */

.hero { padding: clamp(48px, 7vw, 96px) 0 clamp(64px, 10vw, 128px); border-top: 0; }
.hero-grid {
  display: grid;
  grid-template-columns: 220px 1fr;
  gap: clamp(24px, 4vw, 56px);
  align-items: center;
}
.hero-portrait img {
  width: 220px;
  height: 220px;
  border-radius: 50%;
  object-fit: cover;
  border: 1px solid var(--border-strong);
  box-shadow: 0 0 0 6px rgba(212, 161, 74, 0.06), 0 12px 40px rgba(0,0,0,0.5);
}
.hero-text { min-width: 0; }
.hero-text h1 { margin-bottom: 16px; }
.hero-tagline {
  font-family: var(--mono);
  font-size: 13px;
  letter-spacing: 0.08em;
  color: var(--text-muted);
  margin: 0 0 4px;
}
.hero-oneliner {
  font-family: var(--serif);
  font-style: italic;
  font-size: clamp(18px, 1.8vw, 22px);
  color: var(--text);
  margin: 0 0 24px;
}

.role-row {
  list-style: none;
  margin: 0 0 16px;
  padding: 0;
  display: flex;
  flex-wrap: wrap;
  gap: 8px 16px;
  align-items: baseline;
}
.role-row .sep { color: var(--border-strong); }
.role-chip {
  display: inline-flex;
  flex-direction: column;
  line-height: 1.2;
}
.role-org { font-size: 14px; color: var(--text); font-weight: 500; }
.role-title { font-family: var(--mono); font-size: 11px; color: var(--text-dim); letter-spacing: 0.06em; text-transform: uppercase; }

.contact-row {
  list-style: none;
  padding: 0;
  margin: 0;
  display: flex;
  gap: 14px;
  align-items: center;
}
.icon-link {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 34px;
  height: 34px;
  border: 1px solid var(--border);
  border-radius: 50%;
  color: var(--text-muted);
  transition: border-color 120ms ease, color 120ms ease, background 120ms ease;
}
.icon-link:hover { border-color: var(--accent); color: var(--accent); background: var(--accent-soft); }
.icon-link img { display: block; opacity: 0.85; }
.icon-link:hover img { opacity: 1; filter: brightness(0) saturate(100%) invert(74%) sepia(45%) saturate(471%) hue-rotate(355deg) brightness(89%) contrast(89%); }

.scroll-hint {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 6px;
  margin-top: clamp(40px, 6vw, 80px);
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: var(--text-dim);
}
.scroll-hint:hover { color: var(--accent); }

@media (max-width: 720px) {
  .hero-grid {
    grid-template-columns: 1fr;
    text-align: left;
    gap: 24px;
  }
  .hero-portrait img { width: 140px; height: 140px; }
}
```

- [ ] **Step 2: Local preview — start the dev server**

```bash
make dev &
```

Expected: server logs `Serving HTTP on :: port 8080`.

- [ ] **Step 3: Smoke check via curl**

```bash
curl -s -o /dev/null -w '%{http_code}\n' http://localhost:8080/
curl -s http://localhost:8080/data/profile.json | python -c "import json,sys;d=json.load(sys.stdin);print('roles:',len(d['roles']))"
```

Expected: `200` then `roles: 5`.

- [ ] **Step 4: Manual visual check**

Open http://localhost:8080 in browser. Verify:
- Topbar with brand + nav + PT|EN toggle.
- Hero: 220px portrait left, big italic name right, 5 roles in a row, 4 contact icons.
- Click PT|EN: nav labels swap. Tagline + oneLiner translate.
- Scroll: smooth, no console errors.

- [ ] **Step 5: Commit**

```bash
git add public/css/style.css
git commit -m "feat(css): style topbar and hero (portrait, name, roles, contacts)"
```

---

### Task 15: Extend `public/css/style.css` — sobre, áreas

**Files:**
- Modify: `public/css/style.css` (append)

- [ ] **Step 1: Append**

```css
/* ============================================================
   Sobre
   ============================================================ */

.about-grid {
  display: grid;
  grid-template-columns: minmax(0, 2fr) minmax(0, 1fr);
  gap: clamp(24px, 4vw, 56px);
  align-items: start;
}
.about-prose p {
  font-size: 16px;
  line-height: 1.7;
  color: var(--text-muted);
}
.about-prose p strong { color: var(--text); font-weight: 500; }
.about-prose p:first-child::first-line {
  font-family: var(--serif);
  font-style: italic;
  color: var(--text);
  font-size: 18px;
  letter-spacing: -0.01em;
}

.about-side h3.eyebrow { margin-bottom: 14px; }
.highlights {
  list-style: none;
  margin: 0;
  padding: 0;
  border-top: 1px solid var(--border);
}
.highlights li {
  display: grid;
  grid-template-columns: 90px 1fr;
  gap: 12px;
  padding: 12px 0;
  border-bottom: 1px solid var(--border);
  font-size: 13px;
  color: var(--text);
}
.highlights .key {
  color: var(--text-dim);
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}
.highlights a { color: var(--accent); }

@media (max-width: 720px) {
  .about-grid { grid-template-columns: 1fr; }
}

/* ============================================================
   Áreas
   ============================================================ */

.section-helper {
  color: var(--text-dim);
  font-size: 13px;
  margin: -16px 0 24px;
}
.area-pills {
  list-style: none;
  margin: 0;
  padding: 0;
  display: flex;
  flex-wrap: wrap;
  gap: 8px 10px;
}
.area-pill {
  background: var(--bg-elev);
  color: var(--text-muted);
  border: 1px solid var(--border);
  border-radius: 999px;
  padding: 6px 14px;
  font-family: var(--mono);
  font-size: 12px;
  letter-spacing: 0.04em;
  cursor: pointer;
  transition: all 120ms ease;
}
.area-pill:hover { border-color: var(--accent); color: var(--text); }
.area-pill.is-active {
  background: var(--accent-soft);
  border-color: var(--accent);
  color: var(--accent);
}
```

- [ ] **Step 2: Manual visual check**

Refresh browser. Verify:
- Sobre section: prose left, sidebar with highlights right.
- Áreas section: row of pills, "all/todas" highlighted.
- Click a pill: it goes active (no project filtering yet visible since projects not styled).

- [ ] **Step 3: Commit**

```bash
git add public/css/style.css
git commit -m "feat(css): style sobre prose+sidebar and area pills"
```

---

### Task 16: Extend `public/css/style.css` — papéis, projetos, ensino

**Files:**
- Modify: `public/css/style.css` (append)

- [ ] **Step 1: Append**

```css
/* ============================================================
   Papéis (atuação)
   ============================================================ */

.roles-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
  gap: clamp(16px, 2.4vw, 28px);
}
.role-card {
  background: var(--bg-elev);
  border: 1px solid var(--border);
  border-radius: 10px;
  padding: 20px 22px;
  transition: border-color 120ms ease, transform 120ms ease;
}
.role-card:hover { border-color: var(--border-strong); }
.role-card header {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  gap: 12px;
}
.role-card header h3 { font-family: var(--serif); font-size: 22px; }
.role-period {
  font-family: var(--mono);
  font-size: 11px;
  color: var(--text-dim);
  letter-spacing: 0.06em;
  text-transform: uppercase;
}
.role-card .role-title {
  display: inline-block;
  margin: 4px 0 12px;
  font-family: var(--mono);
  font-size: 11px;
  color: var(--accent);
  letter-spacing: 0.08em;
  text-transform: uppercase;
}
.role-card p { font-size: 14px; line-height: 1.6; }
.chip-row {
  list-style: none;
  margin: 12px 0 12px;
  padding: 0;
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
}
.chip {
  display: inline-block;
  background: var(--bg-elev-2);
  color: var(--text-muted);
  border: 1px solid var(--border);
  border-radius: 4px;
  padding: 3px 8px;
  font-family: var(--mono);
  font-size: 11px;
}
.chip:hover { color: var(--accent); border-color: var(--accent-line); }
.role-link {
  display: inline-block;
  margin-top: 10px;
  font-family: var(--mono);
  font-size: 12px;
  color: var(--text-dim);
}
.role-link:hover { color: var(--accent); }

/* ============================================================
   Projetos em destaque
   ============================================================ */

.projects-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
  gap: clamp(16px, 2vw, 24px);
}
.project-card {
  background: var(--bg-elev);
  border: 1px solid var(--border);
  border-radius: 10px;
  padding: 18px 20px;
  display: flex;
  flex-direction: column;
  gap: 10px;
  transition: border-color 120ms ease;
}
.project-card[hidden] { display: none; }
.project-card:hover { border-color: var(--accent-line); }
.project-card h3 { font-family: var(--mono); font-size: 14px; letter-spacing: 0; }
.project-card h3 a { color: var(--text); }
.project-card h3 a:hover { color: var(--accent); }
.project-card p {
  font-size: 13px;
  margin: 0;
  color: var(--text-muted);
  line-height: 1.55;
}
.project-tags { display: flex; gap: 6px; flex-wrap: wrap; }
.tag {
  display: inline-block;
  font-family: var(--mono);
  font-size: 10px;
  letter-spacing: 0.06em;
  padding: 2px 8px;
  border-radius: 4px;
  border: 1px solid var(--border);
  color: var(--text-dim);
  background: transparent;
}
.tag-area { background: var(--accent-soft); color: var(--accent); border-color: var(--accent-line); }
.tag-mono { color: var(--text-muted); }
.stack-row {
  list-style: none;
  padding: 0;
  margin: 0;
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
}
.project-foot {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-top: auto;
  padding-top: 10px;
  font-family: var(--mono);
  font-size: 11px;
  color: var(--text-dim);
  border-top: 1px solid var(--border);
}
.project-foot .stars { color: var(--accent); }
.project-foot a { color: var(--text-muted); }
.project-foot a:hover { color: var(--accent); }
.project-foot .demo-link::before { content: "· "; color: var(--text-dim); }

/* ============================================================
   Ensino, pesquisa & divulgação
   ============================================================ */

.teaching-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
  gap: clamp(20px, 3vw, 36px);
}
.teach-block h3.eyebrow { margin-bottom: 12px; color: var(--accent); }
.teach-block ul {
  list-style: none;
  padding: 0;
  margin: 0;
  border-top: 1px solid var(--border);
}
.teach-block li {
  font-size: 13.5px;
  color: var(--text);
  padding: 10px 0;
  border-bottom: 1px solid var(--border);
  line-height: 1.45;
}
```

- [ ] **Step 2: Manual visual check**

Refresh browser. Verify:
- Atuação: 5 cards, each with org+period header, role-title in mono ouro, description, chips, role-link.
- Projetos: 9 cards in responsive grid, each with area tags, name in mono, description, stack chips, footer with star/lang/links.
- Click an area pill — projects should filter; click "all/todas" to reset.
- Ensino: 5 blocks in a grid (Disciplinas, Material aberto, Webinars, Programas coordenados, Orientações ativas).

- [ ] **Step 3: Commit**

```bash
git add public/css/style.css
git commit -m "feat(css): style role cards, project cards, and teaching grid"
```

---

### Task 17: Extend `public/css/style.css` — publicações, open source table, contato, footer

**Files:**
- Modify: `public/css/style.css` (append)

- [ ] **Step 1: Append**

```css
/* ============================================================
   Publicações
   ============================================================ */

.pub-list {
  list-style: none;
  margin: 0;
  padding: 0;
  border-top: 1px solid var(--border);
}
.pub-item {
  display: grid;
  grid-template-columns: 80px 1fr;
  gap: 16px;
  padding: 18px 0;
  border-bottom: 1px solid var(--border);
}
.pub-year {
  font-family: var(--serif);
  font-style: italic;
  font-size: 28px;
  color: var(--accent);
  line-height: 1;
}
.pub-title { font-size: 15px; line-height: 1.45; color: var(--text); margin-bottom: 4px; }
.pub-title a { color: var(--text); border-bottom: 1px solid var(--border-strong); }
.pub-title a:hover { color: var(--accent); border-color: var(--accent); }
.pub-meta {
  display: flex;
  align-items: center;
  gap: 10px;
  font-family: var(--mono);
  font-size: 11px;
  color: var(--text-muted);
  margin-bottom: 4px;
}
.pub-first { color: var(--accent); border-color: var(--accent-line); }
.pub-authors {
  font-family: var(--mono);
  font-size: 11px;
  color: var(--text-dim);
  letter-spacing: 0.02em;
}
.pub-cta { margin-top: 24px; }
.pub-cta a {
  font-family: var(--mono);
  font-size: 13px;
  color: var(--accent);
  border-bottom: 1px solid var(--accent-line);
}
.pub-cta a:hover { border-color: var(--accent); }

/* ============================================================
   Open source
   ============================================================ */

.repo-controls {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 12px 18px;
  margin-bottom: 20px;
}
#repo-search {
  flex: 1 1 220px;
  background: var(--bg-elev);
  border: 1px solid var(--border);
  color: var(--text);
  padding: 8px 12px;
  border-radius: 6px;
  font-family: var(--mono);
  font-size: 12px;
}
#repo-search::placeholder { color: var(--text-dim); }
#repo-search:focus { outline: none; border-color: var(--accent-line); }
.recent-toggle {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  font-family: var(--mono);
  font-size: 12px;
  color: var(--text-muted);
  cursor: pointer;
}
.recent-toggle input { accent-color: var(--accent); }
.lang-pills {
  list-style: none;
  margin: 0;
  padding: 0;
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
}
.lang-pill {
  background: var(--bg-elev);
  border: 1px solid var(--border);
  color: var(--text-muted);
  border-radius: 999px;
  padding: 4px 10px;
  font-family: var(--mono);
  font-size: 11px;
  cursor: pointer;
}
.lang-pill:hover { color: var(--text); border-color: var(--border-strong); }
.lang-pill.is-active { background: var(--accent-soft); border-color: var(--accent); color: var(--accent); }

.repo-table-wrap { overflow-x: auto; }
.repo-table {
  width: 100%;
  border-collapse: collapse;
  font-size: 13px;
}
.repo-table th {
  text-align: left;
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--text-dim);
  font-weight: 500;
  padding: 10px 12px;
  border-bottom: 1px solid var(--border);
}
.repo-table td {
  padding: 9px 12px;
  border-bottom: 1px solid var(--border);
  vertical-align: top;
}
.repo-table tr[hidden] { display: none; }
.repo-table .mono { font-family: var(--mono); font-size: 12px; }
.repo-table .num { text-align: right; color: var(--accent); }
.repo-table .dim { color: var(--text-dim); }
.repo-table tr:hover td { background: rgba(212, 161, 74, 0.03); }
.repo-empty {
  font-family: var(--mono);
  font-size: 12px;
  color: var(--text-dim);
  margin-top: 14px;
  text-align: center;
}

/* ============================================================
   Contato + footer
   ============================================================ */

.contact-cta {
  font-family: var(--serif);
  font-style: italic;
  font-size: clamp(20px, 2vw, 26px);
  color: var(--text);
  max-width: 60ch;
  margin: -8px 0 32px;
}
.contact-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
  gap: clamp(20px, 3vw, 40px);
}
.contact-list { list-style: none; margin: 0; padding: 0; border-top: 1px solid var(--border); }
.contact-list li {
  display: grid;
  grid-template-columns: 100px 1fr;
  gap: 12px;
  padding: 10px 0;
  border-bottom: 1px solid var(--border);
}
.contact-list .key {
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.08em;
  color: var(--text-dim);
  text-transform: uppercase;
}
.contact-list a { color: var(--text); }
.contact-list a:hover { color: var(--accent); }

.foot {
  border-top: 1px solid var(--border);
  margin-top: 40px;
}
.foot-inner {
  display: flex;
  justify-content: space-between;
  align-items: center;
  height: 64px;
  font-family: var(--mono);
  font-size: 11px;
  color: var(--text-dim);
}
.foot-inner a { color: var(--text-muted); border-bottom: 1px dotted var(--border-strong); }
.foot-inner a:hover { color: var(--accent); border-color: var(--accent); }
```

- [ ] **Step 2: Manual visual check**

Refresh browser. Verify:
- Publicações: list with year ouro itálico left, paper title + venue + authors right, "first author" tag visible on the 2022 paper.
- Open source: search input + recent toggle + language pills above. Table with all repos. Sort by stars desc — IA-FIC-IFSP, hey-jarvis, Webinar-VC near top.
- Type in search: rows filter live. Click a language pill: rows filter. Toggle "ativo 12 meses": old repos hide.
- Contato: cta italic, two columns (emails + perfis).
- Footer: copyright + build SHA short, both columns.

- [ ] **Step 3: Stop dev server (will restart later)**

Bring `make dev` to foreground and Ctrl+C.

- [ ] **Step 4: Commit**

```bash
git add public/css/style.css
git commit -m "feat(css): style publications list, open-source table, contact, footer"
```

---

## Phase 6 — SEO + OG

### Task 18: OG image (text-to-PNG via Python+Pillow)

**Files:**
- Create: `public/assets/og-image.png`

This is a one-time generated image (1200×630 social card). We use Pillow if available; otherwise fall back to a manual export from Figma/Canva. The plan uses Pillow.

- [ ] **Step 1: Generate the OG image with Pillow**

```bash
python - <<'PY'
from PIL import Image, ImageDraw, ImageFont
import os, sys

W, H = 1200, 630
bg, fg, accent, dim = (13,15,18), (232,233,234), (212,161,74), (155,160,168)
img = Image.new("RGB", (W, H), bg)
d = ImageDraw.Draw(img)

# subtle radial — approximated with two ellipses
for r, alpha in [(700, 14), (500, 24)]:
    d.ellipse((W-700, -200, W+200, 500), fill=(int(bg[0]+(accent[0]-bg[0])*alpha/255),
                                                 int(bg[1]+(accent[1]-bg[1])*alpha/255),
                                                 int(bg[2]+(accent[2]-bg[2])*alpha/255)))
# overlay restore base
d.rectangle((0, 0, W, H), outline=None, fill=None)

# load fonts (fallback to default if not installed)
def font(name_candidates, size):
    for n in name_candidates:
        try: return ImageFont.truetype(n, size)
        except OSError: continue
    return ImageFont.load_default()

serif = font(["Georgia.ttf", "Georgia", "C:/Windows/Fonts/georgia.ttf",
              "C:/Windows/Fonts/georgiai.ttf"], 96)
serif_it = font(["C:/Windows/Fonts/georgiai.ttf", "Georgia Italic"], 96)
sans = font(["C:/Windows/Fonts/seguibl.ttf", "C:/Windows/Fonts/segoeui.ttf",
             "Arial.ttf"], 28)
mono = font(["C:/Windows/Fonts/consola.ttf", "Consolas"], 22)

# left content
x = 80
d.text((x, 110), "Gustavo", fill=fg, font=serif)
d.text((x + 360, 110), "von", fill=accent, font=serif_it)
d.text((x + 540, 110), "Atzingen", fill=fg, font=serif)

d.text((x, 260), "Professor · Pesquisador · Engenheiro", fill=dim, font=sans)
d.text((x, 320), "Computer vision, EEG, atuarial e sistemas para educação e governo.", fill=fg, font=sans)

roles = "IFSP · CEPAD-IFSP · Plataforma Nilo Peçanha · Quickium · Inteligência Atuarial"
d.text((x, 480), roles, fill=accent, font=mono)
d.text((x, 540), "atzingen.dev", fill=dim, font=mono)

# round portrait
try:
    p = Image.open("public/assets/rosto.jpeg").convert("RGB").resize((280, 280))
    mask = Image.new("L", (280, 280), 0)
    ImageDraw.Draw(mask).ellipse((0, 0, 280, 280), fill=255)
    img.paste(p, (W - 360, 175), mask)
except FileNotFoundError:
    pass

img.save("public/assets/og-image.png", "PNG", optimize=True)
print("wrote public/assets/og-image.png")
PY
```

Expected: `wrote public/assets/og-image.png`. File is ~80–250 KB.

- [ ] **Step 2: If Pillow not installed**

```bash
pip install Pillow
```

Then re-run Step 1.

- [ ] **Step 3: Verify file**

```bash
ls -la public/assets/og-image.png
```

Expected: file exists, > 30 KB.

- [ ] **Step 4: Commit**

```bash
git add public/assets/og-image.png
git commit -m "feat(seo): add OG/Twitter card image"
```

---

### Task 19: Local end-to-end smoke test

- [ ] **Step 1: Restart dev server and run a build to refresh data**

```bash
python build.py
make dev &
sleep 1
```

- [ ] **Step 2: Curl smoke checks**

```bash
curl -s -o /dev/null -w 'index: %{http_code}\n' http://localhost:8080/
curl -s -o /dev/null -w 'css:   %{http_code}\n' http://localhost:8080/css/style.css
curl -s -o /dev/null -w 'i18n:  %{http_code}\n' http://localhost:8080/data/i18n.json
curl -s -o /dev/null -w 'profile:  %{http_code}\n' http://localhost:8080/data/profile.json
curl -s -o /dev/null -w 'projects: %{http_code}\n' http://localhost:8080/data/projects.json
curl -s -o /dev/null -w 'pubs:     %{http_code}\n' http://localhost:8080/data/publications.json
curl -s -o /dev/null -w 'repos:    %{http_code}\n' http://localhost:8080/data/repos.json
curl -s -o /dev/null -w 'build:    %{http_code}\n' http://localhost:8080/data/build.json
curl -s -o /dev/null -w 'rosto:    %{http_code}\n' http://localhost:8080/assets/rosto.jpeg
```

Expected: all `200`.

- [ ] **Step 3: Browser manual check (cold load)**

Open http://localhost:8080 in a browser. Verify the entire scroll once:
1. Hero loads with portrait, name italic ouro on "von", 5 roles, 4 contact icons.
2. PT|EN toggle: nav + tagline + bio re-render. Persists on reload.
3. Sobre prose has 3 paragraphs.
4. Áreas pills + click filters Projetos.
5. Atuação: 5 cards visible.
6. Projetos: 9 cards. Click "Computer Vision" pill → only 3-4 cards visible.
7. Click "all" → all 9 visible again.
8. Ensino: 5 blocks.
9. Publicações: 9 entries with years ouro.
10. Open Source table loads ≥30 rows. Type "eeg" in search → 1-2 rows. Click "Python" pill → most rows. Toggle "ativo 12 meses" → fewer.
11. Contato: emails + GitHub + Lattes + LinkedIn links.
12. Footer: build SHA visible, links to GitHub commit.
13. No console errors.

If any check fails, fix and re-test before continuing.

- [ ] **Step 4: Lighthouse (optional, if Chrome local)**

In Chrome DevTools → Lighthouse → Mobile → Generate report. Target ≥ 95 on Performance + Accessibility + SEO.

- [ ] **Step 5: Stop dev server, commit any fixes**

```bash
# Ctrl+C the dev server
git status   # should be clean if no fixes were needed
```

If fixes were committed, message: `fix(...): describe`.

---

## Phase 7 — GitHub repo + first push

### Task 20: Create GitHub repo and push

- [ ] **Step 1: Confirm gh auth**

```bash
gh auth status
```

Expected: logged in as `Atzingen`.

- [ ] **Step 2: Create the public repo**

```bash
gh repo create Atzingen/atzingen-dev \
  --public \
  --description "Personal portfolio at https://atzingen.dev — static site, bilingual, dark utility look." \
  --homepage "https://atzingen.dev"
```

Expected: `✓ Created repository Atzingen/atzingen-dev on GitHub`.

- [ ] **Step 3: Add the remote and push**

```bash
git remote add origin git@github.com:Atzingen/atzingen-dev.git
git push -u origin main
```

Expected: pushes all commits, first remote tracking branch set.

- [ ] **Step 4: Verify on GitHub**

```bash
gh repo view Atzingen/atzingen-dev --json url,description,homepageUrl
```

Expected: JSON with the right URL/description.

---

## Phase 8 — Server provisioning (debian-dockers)

### Task 21: Create deploy key on server, register on GitHub, clone

**Files:** none locally; remote operations on `debian-dockers-deployer`.

- [ ] **Step 1: Create directory and deploy key on server**

```bash
ssh debian-dockers-deployer 'set -e
  sudo mkdir -p /var/local/apps/atzingen-dev
  sudo chown deployer:deployer /var/local/apps/atzingen-dev
  cd ~
  if [ ! -f ~/.ssh/atzingen-dev_deploy_key ]; then
    ssh-keygen -t ed25519 -f ~/.ssh/atzingen-dev_deploy_key -N "" -C "atzingen-dev@$(hostname)"
  fi
  cat ~/.ssh/atzingen-dev_deploy_key.pub'
```

Save the printed public key for the next step.

- [ ] **Step 2: Register the deploy key on GitHub (read-only)**

From local machine:

```bash
ssh debian-dockers-deployer 'cat ~/.ssh/atzingen-dev_deploy_key.pub' \
  | gh repo deploy-key add - --repo Atzingen/atzingen-dev --title "debian-dockers (deployer)"
```

Expected: `✓ Deploy key created`.

- [ ] **Step 3: Configure SSH alias on server**

```bash
ssh debian-dockers-deployer 'cat >> ~/.ssh/config <<EOF

Host github.com-atzingen-dev
  HostName github.com
  User git
  IdentityFile ~/.ssh/atzingen-dev_deploy_key
  IdentitiesOnly yes
EOF
chmod 600 ~/.ssh/config'
```

- [ ] **Step 4: Test the deploy key**

```bash
ssh debian-dockers-deployer 'ssh -T -o StrictHostKeyChecking=accept-new github.com-atzingen-dev || true'
```

Expected: `Hi Atzingen/atzingen-dev! You've successfully authenticated, but GitHub does not provide shell access.`

- [ ] **Step 5: Clone the repo**

```bash
ssh debian-dockers-deployer '
  git clone git@github.com-atzingen-dev:Atzingen/atzingen-dev.git /var/local/apps/atzingen-dev
  cd /var/local/apps/atzingen-dev && ls public/'
```

Expected: clone succeeds, `public/` listing shows index.html, css/, js/, etc.

- [ ] **Step 6: First build on server**

```bash
ssh debian-dockers-deployer '
  cd /var/local/apps/atzingen-dev
  python3 build.py
  ls -la public/data/repos.json public/data/build.json'
```

Expected: both files generated, `repos.json` non-empty.

NB — `gh` CLI must be installed on `debian-dockers`. If not, run:
```bash
ssh -t debian-dockers 'sudo apt-get update && sudo apt-get install -y gh'
ssh debian-dockers-deployer 'gh auth status || gh auth login --with-token < /path/to/token'
```
Authentication flow: prefer **`gh auth login`** interactively if possible; otherwise use a fine-grained PAT with `read:public_repo` only and store via `gh auth login --with-token`.

---

### Task 22: Configure nginx vhost for atzingen.dev

**Files (on server):**
- Create: `/etc/nginx/sites-available/atzingen.dev`
- Symlink: `/etc/nginx/sites-enabled/atzingen.dev`

- [ ] **Step 1: Verify DNS resolves to the home network**

From local machine:

```bash
nslookup atzingen.dev 1.1.1.1
nslookup www.atzingen.dev 1.1.1.1
nslookup fii-alert.atzingen.dev 1.1.1.1
```

Expected: all three return `189.98.101.26`. (Confirmed clean as of 2026-05-07: GoDaddy zone has `A @ → 189.98.101.26`, `A fii-alert → 189.98.101.26`, `CNAME www → atzingen.dev.`. NS `ns17/ns18.domaincontrol.com.` and SOA are GoDaddy defaults.) If any host fails to resolve, troubleshoot at the registrar before continuing.

- [ ] **Step 2: Verify FII still works (regression baseline)**

```bash
curl -s -o /dev/null -w 'fii-alert: %{http_code}\n' https://fii-alert.atzingen.dev/
```

Expected: `200`.

- [ ] **Step 3: Write nginx vhost (HTTP only first, for cert acquisition)**

```bash
ssh -t debian-dockers 'sudo tee /etc/nginx/sites-available/atzingen.dev > /dev/null <<EOF
# atzingen.dev — apex + www on host nginx, static site
server {
    listen 80;
    listen [::]:80;
    server_name atzingen.dev www.atzingen.dev;

    include snippets/letsencrypt.conf;

    root /var/local/apps/atzingen-dev/public;
    index index.html;

    # canonical: redirect www → apex (added after cert in step 6)
    # placeholder for now: serve same content on both during cert acquisition

    location / {
        try_files \$uri \$uri/ =404;
    }

    location ~* \.(?:css|js|svg|png|jpe?g|webp|ico|woff2?)\$ {
        expires 7d;
        add_header Cache-Control "public, max-age=604800, immutable";
    }

    location ~ /\.(?!well-known) { deny all; }
}
EOF'
```

- [ ] **Step 4: Enable + test + reload**

```bash
ssh -t debian-dockers 'sudo ln -sf /etc/nginx/sites-available/atzingen.dev /etc/nginx/sites-enabled/atzingen.dev
sudo nginx -t
sudo systemctl reload nginx'
```

Expected: `nginx: configuration file ... test is successful`.

- [ ] **Step 5: Smoke check HTTP**

```bash
curl -s -o /dev/null -w 'apex-http: %{http_code}\n' http://atzingen.dev/
curl -s -o /dev/null -w 'www-http:  %{http_code}\n' http://www.atzingen.dev/
curl -s -o /dev/null -w 'fii-still: %{http_code}\n' https://fii-alert.atzingen.dev/
```

Expected: apex/www HTTP `200`, fii still `200`.

- [ ] **Step 6: Acquire Let's Encrypt cert**

```bash
ssh -t debian-dockers 'sudo certbot --nginx -d atzingen.dev -d www.atzingen.dev \
  --non-interactive --agree-tos --redirect \
  -m gustavo.von.atzingen@gmail.com'
```

Expected:
- Certificate obtained.
- nginx config rewritten with HTTPS listeners on 443 and HTTP→HTTPS redirect.

- [ ] **Step 7: Add www → apex 301 redirect**

Edit `/etc/nginx/sites-available/atzingen.dev` to add a separate `server` block at the top of the HTTPS section that redirects `www` to apex. Use `sed` so we don't accidentally clobber certbot's edits:

```bash
ssh -t debian-dockers 'sudo bash -c "cat > /etc/nginx/sites-available/atzingen.dev <<EOF
server {
    listen 80;
    listen [::]:80;
    server_name atzingen.dev www.atzingen.dev;
    include snippets/letsencrypt.conf;
    return 301 https://atzingen.dev\$request_uri;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name www.atzingen.dev;

    ssl_certificate /etc/letsencrypt/live/atzingen.dev/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/atzingen.dev/privkey.pem;

    return 301 https://atzingen.dev\$request_uri;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name atzingen.dev;

    ssl_certificate /etc/letsencrypt/live/atzingen.dev/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/atzingen.dev/privkey.pem;

    root /var/local/apps/atzingen-dev/public;
    index index.html;

    location / { try_files \\\$uri \\\$uri/ =404; }

    location ~* \\.(?:css|js|svg|png|jpe?g|webp|ico|woff2?)\\\$ {
        expires 7d;
        add_header Cache-Control \\\"public, max-age=604800, immutable\\\";
    }

    location ~ /\\.(?!well-known) { deny all; }
}
EOF
nginx -t && systemctl reload nginx"'
```

- [ ] **Step 8: Final smoke check**

```bash
curl -sI https://atzingen.dev/ | head -1
curl -sI https://www.atzingen.dev/ | head -1
curl -s https://atzingen.dev/data/profile.json | python -c "import json,sys;d=json.load(sys.stdin);print('roles:',len(d['roles']))"
curl -sI https://fii-alert.atzingen.dev/ | head -1
```

Expected:
- `HTTP/2 200` for atzingen.dev/
- `HTTP/2 301` (or 200 then redirect chain) for www
- `roles: 5`
- `HTTP/2 200` for fii-alert (untouched).

---

### Task 23: Document the manual deploy steps

**Files:**
- Create: `docs/DEPLOY.md`

- [ ] **Step 1: Write the doc (concise summary of Tasks 21-22)**

```markdown
# Deploy manual — atzingen.dev

Setup one-time (já executado em 2026-05-07 em `debian-dockers`).
Reproduzível em qualquer servidor Debian/Ubuntu com nginx + certbot + gh.

## Servidor de referência

- **Host**: `debian-dockers` (10.137.0.30) — atrás do NAT 80/443 do pfSense.
- **Path da app**: `/var/local/apps/atzingen-dev/`.
- **User**: `deployer` (apps), `gustavo` (admin para tarefas com sudo).
- **Domínio**: `atzingen.dev` (apex) + `www.atzingen.dev` (redirect para apex).
- **DNS apex**: `A @ → 189.98.101.26 TTL 600` (Hostinger registrar).

## Pré-requisitos no servidor

- nginx + certbot (`apt install nginx certbot python3-certbot-nginx`)
- snippet `/etc/nginx/snippets/letsencrypt.conf` (já presente)
- `gh` CLI autenticado no usuário `deployer` para `make build`
- Python 3.10+

## 1. Deploy key SSH

```bash
ssh deployer@debian-dockers
ssh-keygen -t ed25519 -f ~/.ssh/atzingen-dev_deploy_key -N "" -C "atzingen-dev@$(hostname)"
cat ~/.ssh/atzingen-dev_deploy_key.pub
```

Cadastrar a chave pública no GitHub:
```bash
gh repo deploy-key add /caminho/atzingen-dev_deploy_key.pub \
  --repo Atzingen/atzingen-dev --title "debian-dockers (deployer)"
```

Adicionar alias SSH em `~/.ssh/config` do deployer:
```
Host github.com-atzingen-dev
  HostName github.com
  User git
  IdentityFile ~/.ssh/atzingen-dev_deploy_key
  IdentitiesOnly yes
```

Test: `ssh -T github.com-atzingen-dev` → `Hi Atzingen/atzingen-dev!`.

## 2. Clone

```bash
git clone git@github.com-atzingen-dev:Atzingen/atzingen-dev.git \
  /var/local/apps/atzingen-dev
cd /var/local/apps/atzingen-dev
python3 build.py
```

## 3. Vhost nginx + cert

Ver Task 22 do plano (`docs/superpowers/plans/2026-05-07-portfolio-implementation.md`)
ou `/etc/nginx/sites-available/atzingen.dev` no servidor para o conteúdo final.

## 4. Atualização (após CI/CD configurado)

GitHub Actions on push a `main`:
- SSH `debian-dockers-deployer`
- `cd /var/local/apps/atzingen-dev && git pull`
- `make build`
- `sudo systemctl reload nginx` (não restart — só pra invalidar cache se mudou vhost)

## Co-existência

`fii-alert.atzingen.dev` continua em produção em `/var/local/apps/fii-scanner/`
(container Flask em port 8500). Vhost separado em
`/etc/nginx/sites-available/fii-alert.atzingen.dev`. Não tocar.
```

- [ ] **Step 2: Commit**

```bash
git add docs/DEPLOY.md
git commit -m "docs: add deploy guide for atzingen.dev"
git push
```

---

## Phase 9 — CI/CD

### Task 24: GitHub Actions deploy workflow

**Files:**
- Create: `.github/workflows/deploy.yml`

- [ ] **Step 1: Add the SSH secrets to the GitHub repo**

From local machine, generate a CI-only SSH keypair (separate from deploy key — this one is *write* into the server, not into GitHub):

```bash
mkdir -p /tmp/atz-ci
ssh-keygen -t ed25519 -f /tmp/atz-ci/ci_key -N "" -C "atzingen-dev-ci"

ssh debian-dockers-deployer 'mkdir -p ~/.ssh && chmod 700 ~/.ssh && touch ~/.ssh/authorized_keys && chmod 600 ~/.ssh/authorized_keys'
ssh debian-dockers-deployer 'tee -a ~/.ssh/authorized_keys' < /tmp/atz-ci/ci_key.pub

gh secret set SSH_HOST_DEBIAN_DOCKERS --body "$(getent hosts debian-dockers | awk '{print $1}' || echo 10.137.0.30)" --repo Atzingen/atzingen-dev
gh secret set SSH_USER_DEBIAN_DOCKERS --body "deployer" --repo Atzingen/atzingen-dev
gh secret set SSH_PRIVATE_KEY_DEBIAN_DOCKERS --body "$(cat /tmp/atz-ci/ci_key)" --repo Atzingen/atzingen-dev

shred -u /tmp/atz-ci/ci_key  # remove local copy of private key
rm -f /tmp/atz-ci/ci_key.pub
rmdir /tmp/atz-ci
```

**Important — `SSH_HOST_DEBIAN_DOCKERS`:** GitHub Actions runners are on the public internet. They cannot reach `10.137.0.30` directly. The home network must expose SSH externally — either via:
- A port-forward 22 → 10.137.0.30 on the pfSense (preferred, since 80/443 are already forwarded), with the secret pointing to the public IP `189.98.101.26`, OR
- A Cloudflare Tunnel / Tailscale runner.

**Confirm with the user before adding the SSH port forward.** If the user prefers to avoid exposing SSH, the deploy can fall back to a *manual* `git pull && make build` until a tunnel is set up.

- [ ] **Step 2: Write the workflow**

```yaml
name: deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Setup ssh agent
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY_DEBIAN_DOCKERS }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H ${{ secrets.SSH_HOST_DEBIAN_DOCKERS }} >> ~/.ssh/known_hosts

      - name: Pull, build and reload
        env:
          HOST: ${{ secrets.SSH_HOST_DEBIAN_DOCKERS }}
          USER: ${{ secrets.SSH_USER_DEBIAN_DOCKERS }}
        run: |
          ssh -i ~/.ssh/id_ed25519 -o StrictHostKeyChecking=yes \
              "$USER@$HOST" "set -e
                cd /var/local/apps/atzingen-dev
                git fetch origin main
                git reset --hard origin/main
                python3 build.py
                # nginx reload only if the deployer has sudo NOPASSWD for it,
                # otherwise this is a no-op for static files (no caching layer needs reload).
                sudo -n systemctl reload nginx 2>/dev/null || true
              "

      - name: Smoke check
        run: |
          set -e
          for host in atzingen.dev www.atzingen.dev; do
            code=$(curl -s -o /dev/null -w '%{http_code}' "https://$host/")
            echo "$host -> $code"
            test "$code" = "200" -o "$code" = "301"
          done
```

- [ ] **Step 3: Commit and push**

```bash
git add .github/workflows/deploy.yml
git commit -m "ci: add GitHub Actions deploy workflow"
git push
```

- [ ] **Step 4: Watch the workflow**

```bash
gh run watch --repo Atzingen/atzingen-dev
```

Expected: green status. If it fails:
- Read logs with `gh run view --log-failed`.
- Most common: `ssh-keyscan` fails because pfSense doesn't forward 22; user needs to confirm the port-forward.

- [ ] **Step 5: Verify the build SHA in the footer**

```bash
curl -s https://atzingen.dev/data/build.json
```

Expected: matches `git rev-parse origin/main`.

---

## Phase 10 — Final acceptance

### Task 25: Run all spec acceptance criteria

- [ ] **Step 1: HTTPS apex returns 200**

```bash
curl -sI https://atzingen.dev/ | head -1
```

Expected: `HTTP/2 200`.

- [ ] **Step 2: www redirects to apex**

```bash
curl -sIL https://www.atzingen.dev/ | grep -E '^(HTTP|location)'
```

Expected: `HTTP/2 301` then `location: https://atzingen.dev/`.

- [ ] **Step 3: FII subdomain still works**

```bash
curl -sI https://fii-alert.atzingen.dev/ | head -1
```

Expected: `HTTP/2 200`.

- [ ] **Step 4: Page renders cold (no cached JSON)**

In a fresh browser profile:
- Open https://atzingen.dev — verify hero loads, 5 roles, photo, contact icons.
- Toggle PT|EN — verify content swap and persistence.
- Click an area pill — verify projects + repo table filter.
- Type in repo search — verify live filtering.

- [ ] **Step 5: Lighthouse mobile**

Run mobile Lighthouse from Chrome DevTools.
Expected: Performance ≥ 95, Accessibility ≥ 95, Best Practices ≥ 95, SEO ≥ 95.
Common fixes if any score drops:
- Performance: ensure images have width/height (already in HTML); enable far-future caching (already in nginx vhost).
- Accessibility: aria-labels on icon links (already done); semantic landmarks (already done).
- SEO: meta description, OG tags (already in `<head>`).

- [ ] **Step 6: OG card preview**

Use https://www.opengraph.xyz/url/https%3A%2F%2Fatzingen.dev or paste the URL into a Slack DM. Verify the card shows photo, name, and tagline.

- [ ] **Step 7: Verify build.json updates on deploy**

Edit `public/data/profile.json` (e.g., add a single space to bio paragraph), commit, push. Wait for Actions to finish, then:

```bash
curl -s https://atzingen.dev/data/build.json
```

Expected: `sha` matches the new commit.

Revert the cosmetic change with another commit.

- [ ] **Step 8: Final commit closing the iteration**

If any final fixes were needed during acceptance:

```bash
git push
```

Otherwise: print a final status line and close the loop.

```bash
echo "✓ atzingen.dev portfolio deployed; spec criteria met as of $(date -Iseconds)."
```

---

## Open follow-ups (not blocking ship)

These are listed in spec section 9 and remain as follow-ups for the user to fill at any point — no need to block deploy:

1. URL oficial **Quickium** — replace `quickium.com` placeholder in `public/data/profile.json`.
2. URL oficial **Inteligência Atuarial** — replace `inteligenciaatuarial.com.br` placeholder.
3. **E-mails comerciais** — extend `contact.emails` array if Quickium/IA emails should appear.
4. **Texto do card Quickium** — replace the placeholder `description` in `roles[3]` of `profile.json`.
5. **Tradução EN da bio** — review `bio.en` paragraphs and adjust voice if needed.
6. **Add favicon `.ico` fallback** — for older browsers (currently we ship only `favicon.svg`).
7. **Optional analytics** — Plausible self-host or similar, when traffic justifies.
